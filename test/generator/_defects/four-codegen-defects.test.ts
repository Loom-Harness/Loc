// Claim stub — the four gates land in the next commits.
// F-010 optional part field, F-017 unconstructible managed field,
// F-021 IdLink guard through a containment row, F-039 `== null` → isNull.
import { describe, it } from "vitest";

describe("four codegen defects", () => {
  it.todo("gates land in the following commits");
});
