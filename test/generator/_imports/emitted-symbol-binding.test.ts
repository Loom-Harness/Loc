// STUB — claim commit for the emitted-import-symbol binding gate.
// Replaced by the real gate in the next commit of this branch.
import { describe, it } from "vitest";

describe.skip("emitted node backend binds every symbol it names", () => {
  it("placeholder", () => {});
});
