# T8 — DX, tooling & the AI platform — completed missions

*Archived 2026-09-02 from [`../T8-dx-tooling-ai.md`](../T8-dx-tooling-ai.md). Every mission below is closed (`done` / `shipped` / `closed` / `concluded` / `withdrawn`); the bodies are moved verbatim (links re-based one level deeper) so the evidence trail stays readable. Nothing here is open work — the live track file lists what remains.*

## M-T8.12 — Playground multi-tab write coordination — `done` · **M** · P2
Two-tab data loss on one workspace closed: per-workspace exclusive Web Lock + read-only loser tab with take-over, BroadcastChannel invalidations through the external-`epoch` machinery; Phase 3 (SharedWorker multi-writer) explicitly rejected. Per-PR gate `web/e2e/multi-tab.spec.ts`. (Body pruned 2026-08-05 per the done-body rule.)
Design: [M-T8.12-multi-tab-coordination-design](missions/M-T8.12-multi-tab-coordination-design.md). Sources: [playground-file-mgmt-review-2026-07](../../audits/playground-file-mgmt-review-2026-07.md) defect #8.

## M-T8.13 — System-builder v1/v2 consolidation — `done` · **L** · P2
One model pane ships: v2 is the single editing surface (owner-gated alternative 4), v1 retired; the shared `usePaneHarness` rails extracted first so both panes rode one source/rev/liveTick/externalTick machinery. (Body pruned 2026-08-05 per the done-body rule.)
Design: [M-T8.13-system-builder-consolidation-design](missions/M-T8.13-system-builder-consolidation-design.md). Sources: [playground-file-mgmt-review-2026-07](../../audits/playground-file-mgmt-review-2026-07.md) §2, [playground-modeller-audit-2026-07](../../audits/playground-modeller-audit-2026-07.md), [playground.md](../../playground.md).

## M-T8.16 — Playground: pipeline strip, one vocabulary, honest dock — `done` (PR #2757) · **M** · P1
The Generate → Bundle → Boot contract is invisible: three controls in three places, disabled with no reason, a different verb on mobile, and dock status carried by 7 px colour dots with no text. One `PipelineStrip` (Validate · Generate · Bundle · Boot, each with state + count, click runs up to it, hover explains a blocker; mobile keeps **Run** with four dots), a `vocabulary.ts` every user-visible string imports from (ratcheted by a literal-grep test), the dock as a real tablist with count badges, a mobile **More** sheet replacing the two-row tab bar, header overflow menu, `forceColorScheme`. Keeps every `btn-*` / `devtools-tab-*` / `mobile-tab-*` test id. Wave 1 of the program; file-disjoint from M-T8.17. Closes audit H1, M1, M3, M4, M7, M11, M12, M16.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../../audits/playground-ux-review-2026-09.md), [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §4 #14, #25.

## M-T8.17 — Playground: confirm / undo layer + the silent no-op drain — `done` (#2758, stacked on the program base) · **M** · P1
The "lost work" class: seven `window.confirm`/`prompt` sites, unconfirmed declaration deletes on the model canvas (while a cosmetic layout reset *does* confirm), no Undo from any visual surface (the Monaco undo stack is correct but unreachable; mobile's `PlainEditor` wipes even the native one), canvas edits discarded on page switch / live re-seed with no dirty flag, and eight `if (next != null) apply(next)` mutations that no-op silently with a refusal line that names nothing. One `ConfirmAction` (inline + modal, type-to-confirm for workspace delete), Undo/Redo in every pane bound to the editor stack, dirty guards, `applyOrRefuse` everywhere with a refusal that names construct + reason + shows the candidate. Wave 1. Closes H8, H9, H10, H11 (M15's keyboard rows stay with M-T8.21's icon-button pass).
Shipped 2026-09-03: `web/src/util/confirm{-state.ts,.tsx}` (catalog + machine + Inline/Modal/Host), `EditorHandle.undo/redo` + `builder/undo-redo.tsx`, craft-history dirty flag + form dirty guard, `harness.on(what)` named writers + `RefusalLine` with *Show candidate*, `add-palette-blockers.ts`. Gates: `test/playground/{confirm,undo-keys,palette-blockers}.test.ts` (the first ratchets `window.confirm|prompt|alert` out of `web/src`), `web/e2e/destructive-actions.spec.ts` (mutation-proved). Found on the way: the file tree's row ⋮ never opened (Tooltip-over-`Menu.Target`; fixed).
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../../audits/playground-ux-review-2026-09.md) §3.2.

## M-T8.18 — Playground: Problems as a teaching surface, `⌘K`, first run — `done` (PR #2767) · **M** · P2
No onboarding, no help link, two keyboard handlers in the whole app, and a Problems list that shows a `loom.*` message with no code, no docs link and no fix even where `fix-hints.ts` has a provider. Problems rows gain code chip + docs anchor (`codeDocsUrl` beside the catalog, ratcheted) + **Fix** (the editor's own `resolvePatchEdits`) + *Ask the agent*, Errors/Warnings toggles, `F8`; a `⌘K` palette over every ctx action (`@mantine/spotlight`, owner-overridable) and a `?` shortcut sheet; a three-door first-run card (describe / example / write) and a `?` header menu; an examples pane by concept with read tracking; the four visual panes' parse-error state gets *Go to line N*. Wave 2 (needs M-T8.16's dock + header slot). Closes H5, H7, M14, M13 (dock).
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §2.2, §4 #18, #20; M-T8.5 (the fix-hint registry it reuses).
**Shipped (#2767).** Problems rows carry a code chip, a docs anchor from the pure `codeDocsUrl` (ratcheted by `test/system/diagnostic-docs-anchors.test.ts`), a **Fix** action over the existing `resolvePatchEdits` path, and *Ask the agent*; `F8`/`Shift+F8` walk the list with an `aria-live` announcement. `⌘K` opens a Spotlight over every ctx action, `?` a shortcut sheet. A three-door first-run card, a `?` header menu and an examples-by-concept pane land the onboarding H5 asked for, and a shared `ParseErrorState` gives all four visual panes a *Go to line N*. Three defects surfaced by the new spec are fixed with it — the stand-in editor handle DROPPED a `revealRange` that arrived before Monaco (so *Write .ddd* and *Go to line N* silently did nothing during the 9.5 MB load; it now queues and replays), the shortcut sheet's test id sat on a Mantine Modal root that stays mounted while closed, and the palette spec asserted a transient `running` state after awaiting the palette's close.

## M-T8.19 — Playground: agent loop as plan → receipt → checkpoint — `done` (PR #2776) · **L** · P2
The builders' agent table stakes, done on the model instead of the transcript: chat as a centre tab with a Split beside Source (the source streaming next to the chat is the demo); a **plan step** that returns a model-node delta (from `loom_outline` of the candidate) the user approves before any `.ddd` is written; a per-turn **receipt** (the `.ddd` diff, the validator delta "2 errors → 0", the generated-file delta, tokens from the provider `usage`); every turn and every visual Apply is a labelled commit with a *Restore* on the message that restores **as a new commit** (undoable, says which point); loop detection after three same-diagnostic fix turns with an "I'm stuck" card; *Fix* code actions labelled free. Rides M-T8.3's transport and the `__loomAgentComplete` seam. Wave 3.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §2.1, §2.4, §4 #3–#5, #12, #13; M-T8.3.
**Shipped (#2776), five slices.** (1) **Chat is a centre tab** (`doc-tab-chat`) beside Source / Builder / Model / Requirements, with a **Split** toggle (`chat-split-toggle`) that shows it next to the editor; the dock's Agent entry is now a SHORTCUT carrying the same `devtools-tab-agent` id, and a persisted `dockTab === "agent"` is coerced to `output`. (2) **The plan step** — `web/src/agent/plan.ts` diffs the `loom_outline` of the candidate against the current one into add/change/remove `PlanItem`s, and `runLiveAgent`'s new `gateSource` holds the whole turn's write until the checklist is approved; a line the user strikes off becomes a REAL `loom_apply_patch` `remove` against the candidate (a `remove` line is all-or-nothing and says so — reverting a deletion needs source text the outline does not carry). Plan-first is on for the first turn of a conversation and for any structural turn, off for a members-only follow-up, with a toggle. (3) **The receipt** — `web/src/agent/receipt.ts` (LCS unified diff with `@@` hunks, diff stat, generated-file delta, tool-call roll-up) plus the compiler's own error counts either side of the write and the provider's `usage`: `Completion.usage` is new on the shared `src/tools` loop and mapped from `prompt_tokens`/`completion_tokens` in both the JSON and SSE paths. (4) **Turn ↔ commit** — an agent turn commits `agent: <first line>` and a visual Apply `builder: <what>` (the harness already carried that label), taken BEFORE the generate so the regenerate commit cannot swallow it; the message's **Restore** restores *as a new commit* and names the point ("the end of turn 2"), and `classifyCommit` grew `agent`/`builder` kinds so History reads the same. (5) **Loop detection** — `web/src/agent/loop-guard.ts` stops after three consecutive fix turns leaving the same code on the same node, with an "I'm stuck" card offering *Restore last green* / *Narrow the ask* / *Open the problem*, and Problems' **Fix** now carries a `free` badge because it is a deterministic patch, not a model call. Strings in `vocabulary.ts` (`CHAT`, `PLAN`, `RECEIPT`, `CHECKPOINT`, `STUCK`, `FREE_ACTION`). Gates: `test/playground/{agent-plan,agent-receipt,agent-loop-guard}.test.ts` (35 cases, all driving the REAL `outline()` / `validate()` / `applyPatches()`), `web/e2e/agent-live.spec.ts` extended to six specs through the key-free `__loomAgentComplete` seam. Mutation-proved twice: bypassing the gate (`applySource` unconditionally) fails `agent-live.spec.ts:76`'s `not.toContain("aggregate Ticket")`, and deleting the trip line in `recordFixTurn` fails 5 of 12 headless loop-guard cases plus the stuck e2e. Found on the way: the existing scripted transports were keyed on the CALL count, but `runAgent` re-calls `complete` with the tool results — so a two-turn script spent its second turn inside the first and single-turn scripts spun to the 12-step cap; they now key on the turn.

## M-T8.20 — Playground: `.loom/` as views, output diff, source ↔ output correspondence — `done` (PR #2777) · **L** · P2
The `.loom/` bundle is generated and then shown as a file tree; the sourcemap that maps every generated line to its `.ddd` span exists for `ddd trace` and is unused by the UI. Explorer switcher grows Diagrams (mermaid live on generate), API (OpenAPI/AsyncAPI), Migrations, Traceability as rendered views; changed-since-last-generate markers and a per-commit generated-output diff; the build worker always records the sourcemap so hovering a declaration highlights the files/lines it produced and hovering generated code flashes the span (godbolt's colour mapping — the category's most memorable feature, across five backends and six frontends); a preview *Select* mode resolving a `data-testid` to its page/primitive. Depends on M-T8.2. Wave 3.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §2.2, §2.3, §4 #15–#17, #23, #24; M-T8.2; [loom-artifacts](../../loom-artifacts.md).
Shipped 2026-09-03 (#2777), four slices.
**1 — views.** The Explorer switcher grows *Diagrams* (the `.loom/*.mmd` files opened in the centre viewer that already renders mermaid), *API* and *Traceability*; the switcher itself became a wrapping button row because six labels do not fit a SegmentedControl in an 18 % column (the `explorer-mode` test id stays on the container, so the six specs matching `getByTestId("explorer-mode").getByText("Generated")` are untouched). `preview/loom-views.ts` classifies the artifacts; `layout/LoomViewsPane.tsx` renders them. The API view was the one that could not just read a file — no static OpenAPI artifact is emitted, the spec is served by the running backend — so the worker derives it from `deriveContextOperations` (`src/ir/util/api-surface.ts`), the derivation all five backend route builders render from, which makes the view correct for a .NET / Java / Phoenix / Python deployable with nothing booted and nothing parsed out of generated source.
**Worker/protocol** (additive): `VirtualFile.hash`, `GenerateOk.api`, and `GenerateOk.sourcemap` — the recorder now runs on EVERY system generate. Returning the map BESIDE `files` is what keeps "always on" free of side effects: `sourceTexts` stays gated on the caller's flag so `src/system/index.ts` skips the v3 / JSR-45 sidecar loops, and the one artifact the recorder adds is stripped back out unless the caller asked for it. The emitted tree, the download `.zip` and every byte-identity fixture are unchanged. **M-T8.2's dependency is satisfied for the playground without any backend fan-out** — the map is construct-granular, so it already covers every target.
**2 — output diff.** `build/output-diff.ts` (pure): per-file added / changed / removed against the previous generate, keyed off the new hash; the generated tree marks rows and a banner summarises the counts (the tree is virtualized, so a change further down has no row mounted to carry a mark). Each History commit grows a collapsed *What changed in the output* grouped by deployable, and the flat commit list is now sources only — a regenerate commit's hundred generated paths used to bury the one `.ddd` edit that caused them. The first generate reports nothing: with no baseline everything would read as "added".
**3 — correspondence.** `build/correspondence.ts` (pure, reusing `src/trace/`'s published wire types + origin walk) answers both directions plus the colour bands. Which lines get painted is decided by the map, not a heuristic: a region whose ORIGIN BEGINS on the hovered line is a declaration the cursor points AT, one that merely covers the line is a declaration the cursor is INSIDE — so an aggregate's head line lights the whole emitted file and a statement inside `operation addLine` lights that operation's own lines instead of the entire class. The mapping is sticky on mouse-leave so the pointer can travel to the Explorer and the open file. Both Monaco surfaces share one injected stylesheet (`editor/correspondence-decorations.ts`), which also owns both highlight TYPES so `EditorPane` / `DesktopShell` never name a module carrying the static monaco import (M-T8.15's boundary).
**4 — preview select mode.** A *Select* toggle arms the running app over the SAME `MessagePort` the runtime forwards use (`{kind:"select-mode"}` down, `{kind:"selected"}` up — no second channel, and the arming stays inside the preview document where it must live once SANDBOX_ORIGIN is a distinct origin). `build/select-target.ts` resolves the clicked `data-testid` to the generated page by matching the EMITTED attribute text — not by reproducing the walker's id-derivation rules, so it holds for every design pack and frontend — then through the sourcemap to the `.ddd` declaration, revealed in the editor with *Open in Builder* / *Ask the agent* beside it.
Gates: `test/playground/correspondence.test.ts` (13 — both directions over a real sourcemap: `aggregate Product` → files in three deployables across two backends, `ui WebApp` → the React frontend's macro-synthesised pages, the anchor rule, the round-trip), `output-diff.test.ts` (8), `select-target.test.ts` (5), `web/e2e/loom-views.spec.ts` (4, no-network) and `web/e2e/preview-select-mode.spec.ts` (heavy lane, self-skipping — the bridge needs a booted preview). Mutation-proved: turning the worker's recorder off fails `loom-views.spec.ts:135`'s `correspondence-banner` assertion. Strings in `vocabulary.ts` (`EXPLORER_VIEW`, `DIAGRAMS`, `API_VIEW`, `TRACEABILITY_VIEW`, `OUTPUT_DIFF`, `CORRESPONDENCE`, `SELECT_MODE`).
**Not built:** the *Migrations* entry the mission's slice 1 listed — M-T8.22 ships that view as a dock tab, and a second door onto it is exactly the vocabulary branching the program exists to stop.

## M-T8.21 — Playground: visual-pane empty states + the scaffold gradient — `done` (PR #2768) · **S** · P2
The Builder tab is empty for the default example because its pages come from `scaffold`, and the copy tells the user to write a `ui { page }` block — the customisation gradient hidden at the one place it should be visible. Scaffold-aware page rows with a badge and an **Unfold** action (the LSP unfold-macro code action through the editor handle); drill-level empty states with a *Back to {parent}* button; the unicode glyph buttons (✎ × ↑ ↓ ƒ ⋯ ⇄) replaced by a labelled inline-SVG set per `ExpressionEditor`'s convention; keyboard-operable requirement rows; middle-ellipsis paths; a detail-level switch in the Model pane. Wave 2 (reuses M-T8.17's primitives and M-T8.18's go-to-line). Closes H6, H7 (panes), M13, M17.
Shipped 2026-09-03 (#2768): `web/src/builder/page/scaffold.ts` (linked build → `enumerateScaffoldPageUnfolds`, the LSP unfold refactor reused as-is — it was already pure) + `BuilderPane` scaffolded rows / Unfold / *Add a page* (`ui-decl.addPage`, declaring the `ui` via the palette's `+ UI` entry when there is none); centred drill-level empty card with *Back to {parent}*, Overview copy says subdomains, settings-rail + empty Match / For / handler hints; `builder/icons.tsx` (Tabler paths, `aria-label` on every control) across ConstructNode / StmtNode / BodyEditor / BuilderPane / StatePanel, keyed collapsed-detail toggles (`rebind` / `clauses`); Requirements rows + `Link` as real buttons, `util/middle-ellipsis.ts`, sentence-case verdicts + `VERDICT_LEGEND` (shared with M-T8.22); `system-v2/detail-level.ts` (Names / Fields / Everything, persisted per view) + the Overview *Used by* line. Strings in `vocabulary.ts` (`SCAFFOLD`, `NO_PAGES`, `MODEL_EMPTY`, `PAGE_SETTINGS`, `VERDICT_LABEL`, `VERDICT_LEGEND`, `DETAIL_LEVEL`, `USED_BY`). Gates: `web/e2e/builder-scaffold.spec.ts` (scaffold list + Unfold, Add a page, detail level + used-by, and a DOM-walk `button-name` check over the three panes — no `@axe-core` dependency), `test/playground/page-builder/scaffold-pages.test.ts`, `test/playground/system-builder/detail-level.test.ts`, `test/playground/middle-ellipsis.test.ts`. Left for M-T8.18: the four "Source has syntax errors" branches.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../../audits/playground-ux-review-2026-09.md), [customization-gradient](../../customization-gradient.md), [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §6.
**Shipped (#2768).** `builder/page/scaffold.ts` lists what each `ui`'s scaffold macro synthesises, so the Builder shows those pages with a badge and an **Unfold** that ejects one into real `.ddd` and selects it — the customization gradient made visible at the one place a user meets it; an empty system offers *Add a page* instead of syntax. Drill-level empty states gained a *Back to {parent}* card, the Overview says `subdomain`, the glyph buttons became a labelled inline-SVG set (`builder/icons.tsx`), requirement rows are keyboard-operable, paths middle-ellipsize, and the Model pane gained a Names/Fields/Everything detail switch plus *Used by*. The accessible-name walk found two real defects with it: the scaffolded-pages Popover had no `trapFocus`, so Escape never reached Mantine's handler and the portal sat over the canvas swallowing clicks, and four clearable `Select`s rendered an unlabelled clear `×`. Mantine's `NumberInput` spinners are the one waiver — no labelling prop in v7, `tabindex="-1"` by construction — narrowed by class so a `tabindex="-1"` button of ours still fails the walk.

## M-T8.22 — Playground: runtime & evolution surfaces — `done` (PR #2762, stacked on the program base) · **M** · P2
The model-driven family's best patterns over data the playground already holds: the Migrations tab rendered as the aggregate diagram tinted by the diff (added / changed / untouched) beside the SQL with destructive ops as a named gate (Prisma Studio's view over `MigrationsIR`); verdicts in sentence case with a legend; a read-only *Tables* view and a *Users* strip in Runtime; requests from the preview counted on the operation nodes in the Model pane with a 404s list (Darklang's traces); every loading state with a failure branch and every raw error with one line of interpretation and a link to the right Output stream. Extends M-T8.11. Wave 2. Closes M8, M9, M10, M18, M19.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §2.3, §4 #22, #26; M-T8.11.
Shipped 2026-09-03 (five slices): **Migrations** — "Compare with" / "Last save", the auto-run's loading line, a Refresh that keeps its label, and no copy that asks for a click the panel already made (M8); the schema drawn as a table diagram tinted by the diff (added / changed / removed / untouched — `web/src/layout/migration-tint.ts` over a worker DTO that now carries `tables` + a per-step `table`) beside the SQL, with a destructive migration rendered as the gate `ddd generate system` raises (`--allow-destructive` + the data it would drop). **Verdicts** — sentence case + one legend (`VERDICT_LABEL` / `VERDICT_LEGEND` in `vocabulary.ts`, shared with M-T8.21); the discovery error gets an interpretation line with the raw text folded (M9, M19). **Runtime tab** — read-only *Tables* (`information_schema` → first 50 rows through `runQuery`) with a *Users* strip (the generated dev stub's built-in identity + the Auth-tab override), and one interpretation line + a See Output → Runtime logs link above every boot / response / dispatch error (M19). **Requests** — `web/src/backend/route-match.ts` matches the runtime log's `request_end` lines to OpenAPI operations; per-operation counts + a 404s list in the Runtime tab; `ctx.requestTraces` exposed for M-T8.20's Model-node counts. **Loading states** — failure branches with Retry on History's commit changes, the baseline list and the pack list; per-suite progress + Cancel on test discovery; the Diagnostics stream in sentences with the ring key kept as a chip (M10, M18). Gates: `test/playground/{route-match,migration-tint,diag-humanize,runtime-views}.test.ts`, `web/e2e/runtime-surfaces.spec.ts` (no-network, mutation-proved), a heavy-lane addition to `web/e2e/runtime.spec.ts` (Tables lists `products`; GET /products counted under Requests — not run in the authoring sandbox). Closes M8, M9, M10, M18, M19; the Migrations-tab half of M-T8.11's surface now lives here.

## M-T8.23 — Playground: targets drawer, read-only links, export — `done` (PR #2775, stacked on the wave-2 base) · **M** · P3
Reach and polish after the loops are right: a Targets drawer that rewrites the deployable clauses (platform / framework / design pack) through `applyPatches` — the compiler playground's version selector applied to the stack; `#view=1` read-only links and an `embed` flag, with the share dialog saying what a link carries; a compose README in the ZIP; the 110-site `dark.N` → semantic-token migration so `forceColorScheme` can go; the audit's L1–L6. Wave 3.
Design: [M-T8.16-playground-ux-program](../missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../../audits/playground-landscape-research-2026-09.md) §4 #19, #21, #29; [api-toolkit](../../api-toolkit.md) (`applyPatches`).

Shipped 2026-09-03 (four slices):

1. **Targets drawer** — `web/src/layout/TargetsDrawer.tsx` over the pure `targets-patch.ts`: every deployable the active file declares, with its platform / frontend framework and its design pack as dropdowns.  A change is a node-addressed `{op:"replace", target:"deployable <name>"}` whose `source` is the declaration's own text with ONE clause rewritten, so comments, clause order and spacing survive and the rest of the file is never re-printed.  The swap carries what it implies (React → Vue also rewrites a bound `ui`'s `framework:` and an incompatible `design:`), a scan that disagrees with the AST refuses rather than rewriting the wrong line, and a no-op refuses visibly.  Menus come from the client-safe `platform/metadata.ts` + `util/builtin-formats.ts`, never `platform/registry.ts`.
2. **Read-only + embed links** — `#view=1` renders without the editing chrome and opens NO workspace store and NO writer lock (`useWorkspace({ viewOnly })`), so following a shared link cannot take a colleague's session; `#embed=1` also drops the dock.  `WorkspaceReadOnlyReason` gains `"view"`; the flags survive the debounced hash rewrite; `ShareDialog` states what the link carries (the `.ddd` source, in the URL) and what it does not (no database rows, no generated files, no saved versions, no settings).  Fixed an adjacent silent bug: the shared-link seed's `"shared"` sentinel had no example entry, so whenever the controller could not hold the payload (a view link, or ephemeral mode) the editor quietly showed the DEFAULT example instead.
3. **Export** — the ZIP gains a root `README.md` (`util/export-readme.ts`) derived from the tree: the service list and each service's published port are read out of the emitted compose file, so it cannot name a service or a port the export lacks.
4. **Theme tokens + the L-tier** — `web/src/theme.css`; all ~110 raw shades migrated, `forceColorScheme` → `defaultColorScheme`.  L1 (one `ReadOnlyBadge`, per-reason label, sentence in the tooltip, only the header adds *Take over*), L2 (Ctrl/⌘+wheel zoom; always-mounted zoom row), L3 ("(Esc)" only under `hover: hover`), L4 (disabled, not absent, toolbar buttons), L6 (header emphasis, with wave 1's `⋯` + labelled auto-run toggle).

Gates: `test/playground/{targets-patch,export-readme,theme-tokens}.test.ts` + 6 cases on `share.test.ts`; `web/e2e/targets-and-share.spec.ts` and `web/e2e/light-scheme.spec.ts` (no-network), `download-zip.spec.ts` extended to pin the README at the ARCHIVE ROOT through its local file header.  Mutation-proved: a no-op `targetPatches` fails `targets-and-share.spec.ts:63`; pinning `--loom-bg-raised` to a bare dark shade fails the theme ratchet by name and drops the light-scheme dock sample to 1.55:1 while the dark control still passes.

**Decisions recorded (both from the program's §4 / §5, unchanged):**
* **Short links — out of scope.**  The playground is a static site with no server, and §5 rules servers out; a shortener needs one.  Hash links plus `#view=1` / `#embed=1` cover sharing, and the share dialog SAYS there is no shortener rather than leaving a long URL looking like an oversight.  Revisit only if link length becomes a field complaint.
* **GitHub push — out of scope.**  It needs a token story the static site has nowhere to keep; the ZIP (now with its compose README) plus the share link are the two supported ways out of the tab.

**Left open, re-homed 2026-09-10 (Wave C0.4):** **L5** (the Agent demo button silently becoming *Replay demo*, `layout/ChatPanel.tsx:116`) is NOT closed, and the same file keeps the single waiver in `theme-tokens.test.ts`'s ratchet (six raw shades).  Both were handed to **M-T8.19**, which closed with [#2776](https://github.com/Loom-Harness/Loc/pull/2776) without taking either; they now live on **[M-T8.3](../T8-dx-tooling-ai.md)**, the mission that owns `ChatPanel.tsx`.
