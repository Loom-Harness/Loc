# Wave C4 — the debt seams and compiler hygiene (coordinator log)

Plan: [`../completion-waves-2026-09.md`](../completion-waves-2026-09.md) §4 "Wave C4". Rules: §3 there (rules 10–18) and §3/§3a of [`../improvement-waves-2026-09.md`](../improvement-waves-2026-09.md). Wave PR: **[#3011](https://github.com/Loom-Harness/Loc/pull/3011)** (draft = the claim) on `claude/loom-review-planning-adz0n4`, restarted from `main` @ `a45fc948b` (the C2 batch-3 merge, 2026-09-22 03:19Z).

## Status: **in progress — 4a, 4c, 4d, 4b and 4e folded (2026-09-22); 4f launched, the last packet**

## Coordinator commit (first, per the kickoff)

This log, plus the reconciliation below. C4 has no register re-classification to land up front (C2's was the `seam` re-class); its coordinator commit is the measured baselines every packet drains FROM, so a packet's exit is a diff against a number this file pins.

## Reconciliation before launch (2026-09-22, on `main` @ `a45fc948b`)

- **Prerequisites.** #2770 and #2778 are merged (C0). Every mission C4 names is still live: M-T9.50 `partial` (config + baseline + ratchet landed, the drain is open), M-T9.56 `open` (gate half landed in C1 1g, the drain is C4's), M-T5.21 `open` (design signed off, [`missions/M-T5.21-callable-unification-design.md`](../missions/M-T5.21-callable-unification-design.md)), M-T9.26 `open` (design [`missions/M-T9.26-route-target-seam-brief.md`](../missions/M-T9.26-route-target-seam-brief.md); unblocked, re-measure post-#2462), M-T5.10 `partial` (the `wireShape` retirement spun off as XL, unminted), M-T5.16 `open`, M-T9.4 `partial`, M-T9.5 `partial`, M-T5.9 `open`, M-T1.13 `open`, M-T9.53 `open`, M-T8.9 `open`, M-T9.32 `partial`. M-T6.18 is `done` (2026-09-10) — the 4e slot is the M-T5.10 measurement, as the plan already says.
- **Baselines measured on this head** (the numbers each packet's exit is diffed against):

| ratchet | file | now |
|---|---|---|
| `test/` typecheck errors / files (M-T9.50) | `test-typecheck-baseline.json` via `node scripts/test-typecheck.mjs` | **469 / 181** at launch → **0 after the 4b fold; the baseline file is deleted and the script is a gate** (largest were: `render-expr-kinds.test.ts` ×15 on typescript, ×12 on python; `api-binding-validation`, `stmt-rows`, `deployable-realization-axes`, `slice0-shell` ×12) |
| uncoded validator sites (M-T9.56) | `test/system/diagnostic-uncoded-baseline.ts` (per-file exact counts, shrink-only, a row at 0 is deleted) | **128 across 12 files** at launch (`deployable.ts` 24, `statements.ts` 22, `ui.ts` 21, `types.ts` 15, `match.ts` 12, `datasource.ts` 9, …) → **0 after the 4c fold; the baseline file is deleted and invariant 5 is an absolute gate** (the plan's "~130" figures in §1/§4/§7 are the pre-drain measurement) |
| `UNDOCUMENTED_CODES` length | `test/system/diagnostic-docs-anchors.test.ts` | **365** |
| `MAX_OPEN_GAPS` / `LATENT_SEAMS` (not C4's, pinned for drift) | `test/system/unsupported-register.test.ts` | 16 / 27 |

- **Open PRs on the fences, cited not duplicated** (`list_pull_requests`, state open, drafts included, 2026-09-22 03:40Z): the validator fence (4c) is touched by #2871 (page-gate diagnostics: `ui-checks`, `ui-page-structure-checks`), #2874 (read-path validators), #2949 (`loom.unknown-primitive-member`), #2942 (page emitter fails open), #2945 (`currentUser` in criteria), #2982 (`id` as a parameter name — a GRAMMAR change, 4d's fence), #2983/#2981/#2966/#2947 (codegen fixes with validator arms). The test fence (4b) is touched by every open PR that adds a test — 4b keeps to typed fixture builders and directory-scoped edits, never a rewrite of a test another PR is changing. 4f's `PreToolUse` hook item touches `.claude/hooks/` (no open PR).
- **Wave 2 follow-through (4a) inputs.** `elixirString` enforcement and the `seed-emit.ts:52` `exStr` duplicate: [`handoffs/wave-1-elixir.md`](handoffs/wave-1-elixir.md) §"Two structural follow-ups"; the seeder contract's readers: [`handoffs/wave-2-seeder-contract.md`](handoffs/wave-2-seeder-contract.md) §"The five readers"; the §F queue rows and the byte-identical corpus re-check: [`wave-2.md`](wave-2.md) and its fold notes.

## Packets

| packet | model | tree fence | rows | state |
|---|---|---|---|---|
| **4b `test/` typecheck drain (M-T9.50)** | Opus | `test/**`, `tsconfig.test.json`, `test-typecheck-baseline.json`, `scripts/test-typecheck.mjs` | 469 → 0 by directory, largest first; typed fixture builders under `test/_helpers/` replace the partial-object-as-full-IR casts (TS2345/TS2322, 66 %); the baseline file deleted in the last commit and the script promoted from ratchet to gate | **folded** (2026-09-22, `claude/c4-typecheck`, nine commits to `290138b28`; **469 → 0**, gate promoted) |
| **4c uncoded validator conditions (M-T9.56)** | Opus | `src/language/validators/**`, `src/ir/validate/checks/**`, `src/diagnostics/**`, `test/system/diagnostic-*`, firing fixtures | 128 → 0 in slices by validator file (largest first: `deployable`, `statements`, `ui`, `types`, `match`, `datasource`); each condition gets a catalog code (`messages.ts`), a `code-docs.ts` anchor (or an `UNDOCUMENTED_CODES` row it then lowers) and a firing fixture; the per-file rows deleted at 0 | **folded** (2026-09-22, `claude/c4-uncoded`, 11 commits to `248b7e284`; **128 → 0**, gate promoted) |
| **4a Wave 2 follow-through** | Opus | `src/generator/**` (NO emission change), `test/system/**`, `scripts/**` | byte-identical re-check of the corpus snapshots on the merged `main`; the `elixirString` enforcement lint as a `test/system` scan (plus `elixirRegexBody`, and the `exStr` duplicate deleted); the §F queue rows F2/F3/F5 flipped `done` with evidence; the seeder contract's remaining readers | **folded** (2026-09-22, `claude/c4-wave2`, `0a00c3723..a5c7a99ac`) |
| **4d callable unification (M-T5.21) + `RouteTarget` (M-T9.26)** | Opus | `src/language/ddd.langium` + `src/language/generated/**`, `src/language/validators/**` (the `CALLABLE_SITES` legality validator only), `src/ir/lower/**`, `src/language/print/**`, `src/generator/_route/` (if built) | M-T5.21 per the signed-off design: one `Callable` production + `CALLABLE_SITES`, byte-identical across all eleven targets; M-T9.26 re-measured post-#2462 on the merged tree — build slice 1 or decline with the measurement, closing the mission either way | **folded** (2026-09-22, `claude/c4-callable`, eight commits to `79eede539`; M-T5.21 Phase 1 landed byte-identical, M-T9.26 re-measured) |
| **4e M-T5.10 `wireShape` retirement — measure, then build or decline** | Opus | `src/ir/enrich/**`, `src/generator/**` (measurement only unless built) | what retirement touches on the merged tree (179 refs / 49+45 files at the spin-off); slice 1 or a decline with the measurement; the XL minted as its own mission or closed | **folded** (2026-09-22, `claude/c4-wireshape`, six commits to `78e46eed7`; DECLINED as D-WIRESHAPE-KEEP, M-T5.39 minted) |
| **4f compiler and repo hygiene** | Opus | per row | M-T5.16 (a)(b)(c) · M-T9.4 residue (A5 `WorkflowIR` facade, A7.4 embed seam, B23, C-mediums) · M-T9.5 (React `stacks/` → `src/platform/react/v{N}/`) · M-T5.9 `with`/`implements` split + codemod · M-T1.13 menu reform · M-T9.53 NUL bytes + the repo-wide check · M-T8.9 static-analysis breadth · M-T9.32 next-free-id check across open branches · the `PreToolUse` push hook running `npx tsc -b` + the test typecheck on the merged tree (improvement-waves 4.1) | **launched** (2026-09-22, `claude/c4-hygiene`, after the 4e fold) |

**Byte-identical gate.** Every 4a/4d/4e/4f refactor diffs the corpus snapshots before and after on the folded tree and states the result in its note (the plan's C4 addendum); a non-identical diff is either justified line by line or reverted.

**Exit** (plan): baseline files 0; uncoded conditions 0; M-T5.21 `done`; M-T9.26 `done` or `declined`; no `G*-C` "same defect, next backend" row in the next review.

## Fold notes

- **4d, 4b and 4e folded third, fourth and fifth in one push** (`origin/main` @ `ca935fb33` — #3001 — merged first; every merge composed on `docs/new-plan/README.md` (regenerated) and, for 4d, `test/system/diagnostic-firing-census.test.ts` (both fixture blocks kept); one typecheck error surfaced by 4b's promoted gate in 4d's `callable-sites.test.ts` — LSP `message` is `string | MarkupContent` — fixed through 4b's `diagText` helper, and the 4c fold's composition of main's `diagnostic-code-coverage.test.ts` (BASELINE 122 → 0, a fixture-fed vacuity guard, mutation-proved) is what 4e's note H3 asked for). **4d** (`claude/c4-callable`, eight commits): **M-T5.21 Phase 1 landed** — three shared grammar fragments (`CallableLeadModifiers` / `CallableSigModifiers` / `CallableGates`) included by all twelve callable rules, legality declared as data in `src/language/callable-sites.ts` (`CALLABLE_SITES`) read by ONE validator raising `loom.callable-modifier-not-allowed-here` with seven `#slug` arms explaining WHY a site excludes a modifier (`audited` on a `domainService` operation went from a bare parse error to a sentence); the seven copies of the callable parameter binder are one `lower/` leaf (`callable-params.ts`) whose one real divergence — whether a `param: T = <expr>` default is lowered — is a parameter, which surfaced two silent DROPS of a parsed default (domain-service operation, workflow `handle`; held at today's behaviour, handed off as a capability decision). **Byte-identical on all eleven targets, and the corpus alone could not prove it**: every corpus fixture is backend-only, so the 395-cell capture was paired with a frontend snapshot over `examples/` + `web/src/examples/` (70 sources / 6,439 files, every frontend extension reached) — **32,863 emitted files, not one byte different**; mutation-proved three ways, and proof 2 caught a vacuous first draft (phase-① parser errors live on `parseResult.parserErrors`, not `doc.diagnostics`). Not landed by decision: the six duplicate `loom.workflow-*` pairs (four raised in `structural.ts`, 4c's fence at the time; two in `workflow-checks.ts`, outside the fence) — handed off as Phase 2 with a mechanical recipe. **M-T9.26 re-measured**: 217 coupling sites / 22 files (up from the design's 183/18); 14 of 16 contract methods multi-use against real call sites, so the anti-criterion does not fire and the seam is justified; slice 1 is blocked only by the design's own quiet-baseline prerequisite (#2918 changes `routes-builder.ts`, slice 1's one file) — mission left `open` with the ⚠ discharged, neither forced `done` nor `declined`. Correction recorded: #2982 carries no grammar hunk. **4b** (`claude/c4-typecheck`, nine commits): **M-T9.50 done — 469 errors / 181 files → 0**, by directory largest first (`test/ir` 106, `test/language` 81, `test/generator` 89, `test/playground` 51, mid-tier 84, `test/system` 43), typed builders in `test/_helpers/` (`ir-builders.ts` `ExprOf<K>`/`StmtOf<K>`/…, `diagnostics.ts` `diagText`/`diagCodes`/`lspCodes` — seventeen suites carried a byte-identical private `codesOf` typed over a parameter no real `Diagnostic` satisfies, `ast.ts`), nothing under `src/**` edited; `test-typecheck-baseline.json` deleted, `scripts/test-typecheck.mjs` a plain gate, `test.yml`'s step renamed. Eleven fixtures were not testing what they claimed (six IR kinds missing from `walk.test.ts`'s exhaustiveness tables, `{ valslugation: true }` — a typo for `validation` — in two React walker suites, two Feliz suites emitting from zero contexts, and `sourcemap.test.ts` slicing the whole source for five assertions because the artifact's span is a tuple); all fixed, `sourcemap` then failed on a copy-pasted token list and was corrected. Three `src/` type defects handed off. **4e** (`claude/c4-wireshape`, six commits): the retirement **DECLINED and inverted** — proposal steps 6–7 already shipped (#1920/#1937; the "179 refs" counted the pure recompute helpers, which is "derive, don't stamp"), the contract records exist for no shipping model (only `scaffoldHandlers` splices them, five fixtures use it), and where they exist they describe a DIFFERENT shape: 25 of 126 contracted nodes diverge, three classes being live emission defects (a `softDeletable` aggregate's `deletedAt` off the record; **every inherited field of an `extends` aggregate missing from the response contract — on java the mapper is generated FROM the record, so `name`/`email` are never serialised**). Recorded as **D-WIRESHAPE-KEEP** (proposed, 48-h default) and re-scoped as **M-T5.39** (open, L, P1) with the census as its body and a shrink-only ratchet `test/system/wire-contract-divergence.test.ts` (25 rows / 6 classes, mutation-proved three ways); M-T5.10 stays `partial` naming its one remaining item. One in-fence slice: `walker-core.ts`'s dead `vo.wireShape` branch removed (corpus vacuous for that site — proved on a targeted probe). Also located for 4f: a raw NUL byte in `structural-checks.ts:1426` that drops the file from every grep-based census. Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors, **`test-typecheck` gate OK (0)**, counts up to date, docs build 0, targeted suites green (4d: 213 files / 2952; 4e: 145 / 1454), full `npm test` redirected (result in the PR body). Ratchets after the fold: typecheck **0 (gate)**, uncoded **0 (gate)**, `UNDOCUMENTED_CODES` 365, wire/contract divergence **25 (new, shrink-only)**, 16/27. Hand-offs: [`wave-c4-4d-callable.md`](handoffs/wave-c4-4d-callable.md), [`wave-c4-4b-typecheck.md`](handoffs/wave-c4-4b-typecheck.md), [`wave-c4-4e-wireshape.md`](handoffs/wave-c4-4e-wireshape.md). **Owner decisions asked**: 4d's four (Phase-2 routing of the six pairs; the two parameter-default drops — refuse or emit; M-T5.21 `partial` vs `open`; `readQuery` single-use), 4b's one (mint a mission for the `scripts/**` JSDoc-vs-fixture gap `allowJs` exposed, or leave it), 4e's one (override D-WIRESHAPE-KEEP within 48 h if step 8 should stay live).
- **4c folded second** (`claude/c4-uncoded`, eleven signed commits to `248b7e284`; `origin/main` @ `677948e51` — #2911, #2919 — merged first; one conflict, `src/diagnostics/code-docs.ts`, both sides' anchor blocks kept). **M-T9.56's drain is closed: 128 uncoded sites across 12 validator files → 0**, seven commits largest file first; **109 codes for 128 sites** (108 minted + `loom.duplicate-theme-block#system-scope` reused where `ddd-validator.ts` restated a rule `composition.ts` owns; seven codes carry several `#slug`s), the two cross-file families (`loom.requires-not-bool` ×5 in 4 files, `loom.function-return-type-mismatch` ×3 in 2) drained as families. Every code: wording in `messages.ts`, a LIVE `code-docs.ts` anchor, a `FIRING_FIXTURES` entry — the docs-anchors pin **held at 365** (nothing parked undocumented), `FIRING_FIXTURES` +106. Two conditions proved UNREACHABLE by trying to write their fixtures (`loom.valueobject-contains-entity` — the grammar admits no `Containment` in a `valueobject`; `loom.containment-foreign-part` — the scope provider never links the ref) and are pinned in `UNREACHABLE_PINS` with the reason; **owner decision: delete both arms or keep the pins**. Three first-draft names tripped `unsupported-register.test.ts`'s NAME scan (`…-not-backend`, `…-on-backend`, `convert-unsupported`) — renamed rather than registered, so 16/27 stayed put, and the naming rule is now a comment at the site. Both gates promoted from shrink-only ratchets to ABSOLUTE: `diagnostic-uncoded-baseline.ts` deleted, invariant 5 fails on the first `accept(...)` without a `code:` (naming file, line, source), the `loom.unknown` assertion dropped its empty `FIXTURES_RAISING_UNKNOWN` waiver; the vacuous-pass guard no longer counts live offenders (it read `> 50`, a ratchet running the wrong way) and drives a source-taking `uncodedSitesInSource` over a fixture of every shape instead. Mutation-proved twice by file copy (a seeded uncoded `accept` in `repository.ts` → *"no diagnostic site reaches the user without a `loom.*` code"* naming `repository.ts:37`; the `requires` gate reverted inline → *"loom.requires-not-bool's fixture raises no uncoded diagnostic"*) — the second needed because the first does not reach the fixture half, which is the evidence the halves are not redundant. Consequence for every open PR: a validator `accept` without a code now fails `tests passed` with the exact site named, instead of growing a baseline row. Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors, `test-typecheck` 469/181 unmoved (4b's), counts up to date, docs build 0, the diagnostic + validation suites 85 files / 1626 green, full `npm test` redirected (result in the PR body). Hand-off: [`handoffs/wave-c4-4c-uncoded.md`](handoffs/wave-c4-4c-uncoded.md).
- **4a folded first** (`claude/c4-wave2`, six signed commits `0a00c3723..a5c7a99ac`; `origin/main` unchanged at `a45fc948b`, the merge clean with no conflict). **The byte-identical re-check is clean in both directions**: `test/fixtures/baseline-output/` (288 files) regenerates byte-for-byte, and a new wide instrument `scripts/capture-corpus-snapshot.mjs` generates every corpus fixture × the five platforms — **395 cells / 26,424 emitted files, zero validation errors, zero throws** — and diffs identical across the packet's one `src/generator/**` hunk; two consecutive captures on an unchanged tree diff clean, which retires the two non-determinisms the wave-2 fold had normalised (`SECRET_KEY_BASE` → `deterministicHex`, the .NET `#line` path). `exStr` had already gone with wave 2's 2.2, but the class had not: `heex-primitives.ts` carried a partial second copy of `escapeHeexAttr` (missing `<`/`>`) over a `.ddd` string literal — a label containing `<` emitted broken markup; it now calls the funnel (byte-identical on all 395 cells). The enforcement is `test/system/target-string-funnel-census.test.ts`, which enumerates POSITIONS (quote-adjacent raw interpolation, inline `.replace` escape chains, `~r/…/` sigil bodies) across elixir/flutter/feliz: elixir enforced at **0**, flutter + feliz on a shrink-only exact per-file baseline (**13 sites / 7 files**, handed off as H1/H2), dotnet/java/python measured as having no interpolating-literal gap; mutation-proved three ways. §F queue: F2 re-confirmed `done`, F3 flipped `PARTIAL → done` (the audit table was the stale copy — the ledger has read `done` since #2770), F4 → M-T4.12 `partial` and F5 → M-T9.39 `open` recorded, not flipped. No seeder reader bypasses the contract; the census's DENOMINATOR was the open half, closed by `test/system/seeder-contract-denominator.test.ts` (mutation-proved with a sixth probe emitter). One edit outside the literal fence, named: `docs/audits/generator-code-review-2026-08-24.md`'s §F status table (the register the flip lives in). Hand-offs H1 (six F#-string escapers with three escape sets — after #2966/#2981), H2 (four Dart escapers, three bodies), H3 (four baselined sites that are safe-but-cross-file), H4 (`elixir/render-expr.ts` renders a non-literal regex argument INSIDE the sigil — silent wrong emission, no owner). **Owner decision asked**: `improvement-waves-2026-09.md` states Wave 2's exit as "F2/F3/F5 read `done`", but F5 is the whole size-M mission M-T9.39 already scheduled by improvement-wave 3.6 / completion-wave 3f — drop F5 from that exit (citing M-T9.39) or keep Wave 2 formally open until it lands. Ratchets unchanged (469/181, 128/12, 365). Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors, `test-typecheck` ratchet OK, counts up to date, docs build 0, targeted suites 452 files / 4542 green, full `npm test` redirected (result in the PR body). Hand-off: [`handoffs/wave-c4-4a-wave2.md`](handoffs/wave-c4-4a-wave2.md).
