# Wave C2 — nothing unsupported on any target (coordinator log)

*Plan: [`../completion-waves-2026-09.md`](../completion-waves-2026-09.md) §4 Wave C2. Base: `main` @ `6d6c1a1` (Wave C1, #2891, merged 2026-09-13 11:13Z). Ten packets, one per target tree (2a–2j), run as Opus agents in isolated worktrees on local `claude/c2-<packet>` branches; the coordinator folds them on `claude/loom-review-planning-adz0n4` (the only branch this coordinator pushes). The plan's "one PR per target tree" shape is honoured **sequentially on that one branch**: a fold batch is pushed, flipped, merged, and the branch restarted from `main` before the next batch — never a ten-tree fold in one PR.*

## Status: **batch 3 FOLDED — all four packets (2l, 2k, 2n, 2m) on the branch (2026-09-21); ready flip + merge in progress** (batch 2 merged as #2933 → `main` @ `5049b6fea` 2026-09-14 16:44Z; batch 1 as `9713ffa18` 05:34Z). The three batch-3 agents were killed by the Opus weekly rate limit on 2026-09-14 ~17:00Z and resumed on 2026-09-20 16:28Z — a six-day pause with no work lost (worktrees intact), but every Flutter/Postgres/hex toolchain in the scratchpad had to be re-fetched.

## Coordinator commit (first, per the kickoff)

`unsupported-register.ts` gains `kind: "seam"`: the 24 rows whose gate set already names every shipping target (or fires only for a context no backend hosts — the "latent seam" / "dormant" / "fires only when no backend deployable hosts" rows) moved off `gap`, so `MAX_OPEN_GAPS` counts LIVE rows only: **51 → 27** (the plan's 24 live + the 3 config-shaped rows — `context-filter-unsupported`, `persistence-mode-unsupported`, `ui-realtime-unsupported` — which packet 2f dispositions: a misuse error is renamed out of the suffix, per the register header). `LATENT_SEAMS = 24` is pinned exactly beside it, and every seam row's `what` must name its full membership set. `openGaps()` unchanged; `latentSeams()` added; `scripts/completion-denominators.mjs` and `scripts/quality-delta.mjs` count the third kind and their tests assert gap + seam + scope = rows.

## Reconciliation before launch (2026-09-13)

Thirty-one PRs are open, most touching a C2 tree. Each packet's prompt names the ones on its fence; the rule is **skip a row an open PR already covers (cite it in the hand-off), and avoid the files it touches where the fix allows** — at fold the coordinator merges `origin/main` and composes. Two open PRs explicitly claim C2 rows and are folded by reference: **#2903** (2f numeric rows: M-T5.23 + M-T5.24, `avg` over money, the `long` ceiling) and **#2886** (M-T6.67a, the `command`-typed workflow `create` param's wire type — closes 1a's `#payload` refusal). #2852 (`envelope`, option B) and #2860 (M-T1.31 F11 `DestroyForm` gate) stay C1's rows. Owner-gated rows (`D-HANDLE-REMOVAL`; `dapper-no-schema-evolution`'s phase-⑨ ruling if `D-DAPPER-SCHEMA` is not in `docs/decisions.md`) are reported, not built.

| tree fence | open PRs touching it (files) |
|---|---|
| 2a elixir | #2906 + #2870 (heex-primitives, heex-walker-core, liveview-emit), #2852 (document/eventsourced/repository-emit, find-controller), #2900 (auth-emit), #2895 + #2904 (migrations-emit), #2886 (openapi-emit), #2903 (query-projections-emit) |
| 2b dotnet | #2852 (common, dapper, repository, find-emit), #2872 (efcore), #2886 (dto-mapping, workflow-emit), #2900 (auth-emit) |
| 2c node | #2872 (repository-find-hydrate, repository-save-builder), #2881 (value-objects), #2886 (routes-builder, workflow-builder), #2894 (workflow-builder), #2899 (schema), #2903 (routes builders), #2900 (auth-emit) |
| 2d java | #2852 (repository), #2886 (openapi-customizer, wire, workflow), #2901 (service), #2903 (channels, query-projection-reads, numeric-codec), #2900 (auth) |
| 2e python | #2881 + #2901 (repository-builder), #2886 (routes-builder, workflows-builder), #2903 (numeric-codec, query-projections-builder), #2900 (auth-emit) |
| 2f cross (`src/ir/**`, `src/generator/_*/**`) | #2871 (ui-gate*, ui-checks), #2885 (optional-member, id-link, table, registry, row-field-type), #2896 (create-state, workflow-checks, validate), #2903 (codec, lower-projection, projection-checks), #2877 (lower-types, default-deny-checks), #2873 + #2884 (lower-expr/-stmt), #2870 + #2860 (ui-checks, ui-page-structure-checks), #2894, #2895 + #2904 + #2899 (migrations-ir), #2874, #2878, #2897, #2886 |
| 2g walker / react / vue / svelte | #2902 (`field-input-array.hbs` in every JSX pack + form-fields-vm), #2885 (data-grid, id-link, table, registry, target), #2894 (react workflow-builder), #2878 (controls) |
| 2h angular | #2905 (tsconfig.hbs ×3), #2894 (api-module, workflows-module) |
| 2i feliz | #2885 + #2860 (feliz-target) |
| 2j flutter | #2885 (flutter-target) |

## Packets

| packet | model | tree fence | rows | state |
|---|---|---|---|---|
| 2a elixir / Phoenix / HEEx | Opus | `src/generator/elixir/**`, `designs/coreComponents/**`, `designs/daisyui/**` | M-T6.59 the `if` statement RENDERED (`record = if … do … record else record end`, the `else` synthesised; seven one-level-deep body probes moved onto `walkStmtsDeep` — the branch rendered while `Repo.update` wrote nothing) and the gate narrowed to four `#slug` sub-shapes incl. a CLOSED branch vocabulary; `static-subpath-405` elixir arm (ledger → done); M-T6.56 F60 derived-reads-derived (gated on `required:` ≡ `serialize/1`), F22 positional `Image`/builtin `Icon`, F61 HEEx `WorkflowForm` field set + `run_<wf>` handler (own commit); `vanilla-document-unsupported` DERIVED-read clause drained; M-T6.2 §13/§14 + M-T6.26 + M-T6.14 DEBT-12 verified drained (M-T6.26 archived); M-T6.3 DECIDED (**D-PHOENIX-FORMAT-GATE**, archived); `F2-W-06` handed off with a measured recipe (collides with #2904); `table-filter-unsupported`, `heex-component-host-state-unsupported`, G2646 pager left for after #2906 (the same load-binding machinery). `MAX_OPEN_GAPS` unchanged (two rows narrowed in place) | **folded** (2026-09-13) — [`handoffs/wave-c2-2a-elixir.md`](handoffs/wave-c2-2a-elixir.md) |
| 2b .NET / EF / Dapper | Opus | `src/generator/dotnet/**` | `dapper-unsupported#deep-scope` BUILT (hierarchical tenancy on Dapper as raw SQL; `DAPPER_UNSUPPORTED` drained to zero, its ratchet pin and an ir-walk-census waiver deleted; a second compile-invisible defect fixed on the way — the registry self-scope `uuid = text` comparison); `#migrations` + `#schema-split`/`#schema-ignored` re-homed to new **M-T2.17** under D-DAPPER-ALTER; `find-predicate-unsupported` on dapper and pairwise F12 dotnet verified already drained (waiver deleted — met 2e's narrowing at the fold as an empty entry); `tph-filter-unsupported` re-classed `scope` under **D-TPH-SUBTYPE-FILTER**, successor **M-T6.72** (`MAX_OPEN_GAPS` → 25 at the fold); M-T5.7's real residue fixed (`<Concrete>Id` aliases `<Root>Id`); M-T6.14 gate correlates against the migration DDL; G2646 dotnet arm measured a no-op → 2f | **folded** (2026-09-13) — [`handoffs/wave-c2-2b-dotnet.md`](handoffs/wave-c2-2b-dotnet.md) |
| 2c node / Hono / Drizzle / MikroORM | Opus | `src/platform/hono/**`, `src/generator/typescript/**` | `audited-returning-operation-unsupported` implemented and retired (`MAX_OPEN_GAPS` 27 → 26); `this.<refColl>.contains(x)` lowers on MikroORM (`find-predicate-unsupported`'s last general narrowing); pairwise F11 re-classed under **D-EMBEDDED-TPH** (`shape: embedded` forced to `ownTable` under TPH; waiver deleted); M-T4.3 item 4, M-T2.10, #2649 verified stale; G2646 node rows given owners; `mikroorm-unsupported#migrations` handed off (one owner with 2b's dapper twin under D-DAPPER-ALTER), `#schema-split`/`#schema-ignored` measured expressible, not built; new ledger row `drizzle-projection-membership-column-arg-crash` | **folded** (2026-09-13) — [`handoffs/wave-c2-2c-node.md`](handoffs/wave-c2-2c-node.md) |
| 2d java / Spring / JPA | Opus | `src/generator/java/**` | M-T6.36 DRAINED (one identifier funnel `java-ident.ts` + `@JsonProperty`/`@RequestParam`/pointer/sort translation at every wire site; register row, gate, catalogue entry and census fixture deleted; `MAX_OPEN_GAPS` → 24 at the fold) and a second silent gap closed on the way (`enum Kind { case }` → a generated `<Enum>.Codec` `AttributeConverter`); M-T4.2 java document-source aggregation runs natively against the `(id, data, version)` table (`PROJECTION_DOCUMENT_AGG_SUPPORTED` and the `#document` message variants gone, corpus row back to ALL); Schemathesis F21/W34 was a wrong rule text — java accepted a JSON number/boolean for a string field (Jackson coercion), fixed at the `WireNumberStrictness` seam, W34 deleted; `render-sql-restriction.ts:32` proved unreachable and made a floor; G2667-D3, F11 and the F2-XB-4 / F2-CB-C7 java arms verified already closed; W27 (F21's dotnet half) handed to 2b/2f; corpus fixture `java-reserved-words.ddd` | **folded** (2026-09-13) — [`handoffs/wave-c2-2d-java.md`](handoffs/wave-c2-2d-java.md) |
| 2e python / FastAPI / SQLAlchemy | Opus | `src/generator/python/**` | pairwise F12 (already fixed on main by F2-CB-C1 — waiver narrowed to dotnet), F13 (the TPH arms now precede the shape arms — a phantom second table, not two missing imports; **D-TPH-BEATS-SHAPE**), F15 (TPH-nullable bool coerced to `.is_(True/False)`); Schemathesis F17-class residue none (booted leg clean); M-T5.14 python arm (import + port + `await`, reusing the workflow builder's resolver); G2646 python arm re-classed `scope` (**D-PYTHON-SINGLE-REALIZATION**); corpus fixture `tph-crossings.ddd` | **folded** first (2026-09-13) — [`handoffs/wave-c2-2e-python.md`](handoffs/wave-c2-2e-python.md); hand-offs: M-T5.14 on dotnet/java/elixir → 2f; `embedded × TPH` fixture half held on F11 (2c); the `loom.es-tph-forced-own-table` widening question → 2f/owner |
| 2f cross-backend contracts | Opus | `src/ir/**`, `src/generator/_*/**`, one file per backend per row | `seed-event-sourced-unsupported`; `sensitive-wire-unsupported` (M-T3.8 2–4 or a `scope` decision); `polymorphic-id-ref-unsupported`; M-T5.14 read-port on four backends; M-T1.11 (c) domain-floor `code`; M-T3.16 goldens; the five `unowned` `scope` rows; the 3 config-shaped `gap` rows renamed out of the suffix; **not** M-T5.23/M-T5.24 (#2903) and **not** M-T5.22 (C5) | **folded** (2026-09-14) — [`handoffs/wave-c2-2f-ir.md`](handoffs/wave-c2-2f-ir.md) |
| 2g shared walker (react / vue / svelte) | Opus | `src/generator/_walker/**`, `src/generator/{react,vue,svelte}/**`, JSX packs under `designs/**` | `frontend-collection-op-unsupported` (M-T1.20, 8 ops on all seven emitters); `toast-message-unsupported` (M-T1.10); `scaffold-filter-param-unsupported` (M-T1.15); `page-form-locals-unsupported` (M-T1.1); `modal-controlled-op-form-unsupported` (M-T1.6); `queryview-lambda-int-plus-literal-concat`; `ui-projection-read-unsupported#not-ui-consumable`; M-T1.8 vue error boundary; M-T1.28 Svelte F50; M-T1.3 Angular `Chart` leg verify | **folded** (2026-09-14) — [`handoffs/wave-c2-2g-walker.md`](handoffs/wave-c2-2g-walker.md) |
| 2h angular | Opus | `src/generator/angular/**`, `designs/{angularMaterial,primeng,spartanNg}/**` | `component-children-unsupported`; the 4 deferred component shapes; M-T1.14; M-T1.11 `modalTitle`; M-T1.12 aria; the 15 `KNOWN_UNREACHABLE` pins re-verified | **folded** (2026-09-14) — [`handoffs/wave-c2-2h-angular.md`](handoffs/wave-c2-2h-angular.md) |
| 2i feliz | Opus | `src/generator/feliz/**` | `feliz-async-effect-unsupported` (F66 classifier promotion); `store-lifetime-target-unsupported#field`; the 10 deferred component shapes; M-T1.16; sourcemap feliz half; `feliz-navbar-ignores-page-requires`; M-T3.9 History | **folded** (2026-09-14) — [`handoffs/wave-c2-2i-feliz.md`](handoffs/wave-c2-2i-feliz.md) |
| 2j flutter | Opus | `src/generator/flutter/**` | `flutter-async-effect-unsupported`; `store-lifetime-target-unsupported#flutter-field`; the 5+ deferred component shapes; M-T1.16; sourcemap flutter half; M-T4.12 (1) authenticated http client; a11y matrix; M-T3.9 History; M-T1.18 M-G seam (verify-first); `KNOWN_FLUTTER_GAPS` register in `allowlist-ratchet` | **folded** (2026-09-14) — [`handoffs/wave-c2-2j-flutter.md`](handoffs/wave-c2-2j-flutter.md) |
| 2k shared walker + JSX/Vue/Svelte/Angular (batch 3) | Opus | `src/generator/_walker/**`, `src/generator/_frontend/**`, `src/generator/{react,vue,svelte,angular}/**`, `designs/**` | `frontend-collection-op-unsupported` (M-T1.20, 8 ops, seam-first — feliz/flutter/HEEx leaves handed off), `toast-message-unsupported` (M-T1.10), `page-form-locals-unsupported` (M-T1.1, the 69-template rename by script), `scaffold-filter-param-unsupported` (M-T1.15), `modal-controlled-op-form-unsupported` (M-T1.6, default: build + D-MODAL-CONTROLLED-OP-FORM), `ui-projection-read-unsupported#not-ui-consumable` (M-T1.3), `frontend-prop-type-unsupported`, the `slot` walker seam 2h handed off, the `Modal { OperationForm }`-in-`For` silent defect, ledger `M-T1.10-handler-vocabulary` + `F2-CFE-10`, the page-primitive-shadowing ruling (default: D-PAGE-PRIMITIVE-SHADOW) | **folded** (2026-09-21, `claude/c2-walker-3`, `f90e5d78f..79d44b442`) |
| 2l feliz + flutter (batch 3) | Opus | `src/generator/feliz/**`, `src/generator/flutter/**`, the two persist codecs, `src/platform/{feliz,flutter}.ts` | `flutter-action-body-unsupported` (M-T1.32, P1), `feliz-async-effect-unsupported` (component host), `async-effect-subject-unsupported` (shape agreed with 2k), **M-T1.34** (a Flutter component holds a `ref`), `store-lifetime-target-unsupported` residue (record shapes, the `url`-tier nullable cell, the F# `optional` arm — ledger `feliz-flutter-persist-codec-asymmetry`), ledger `F2-CFE-11` flutter half, `M-T1.20-feliz-match-await`, the feliz deferred component shapes (three fixes) | **folded** (2026-09-20, `claude/c2-selfhost-3`, `aa415bdfd..b87ed395f`) |
| 2m elixir / Phoenix / HEEx (batch 3) | Opus | `src/generator/elixir/**`, `designs/coreComponents/**`, `designs/daisyui/**` | `heex-component-host-state-unsupported` (M-T1.27, on #2906's `QueryBinding.guard`), `table-filter-unsupported` HEEx half, `vanilla-document-unsupported` residue (M-T6.35 elixir), `elixir-if-stmt-unsupported`'s four sub-shapes (M-T6.59), ledger **`F2-MT640-SORT-DEAD` (the one open P1)**, `G2646-open-heex-layout-inert` (P2), `schemathesis-F11-int32-range` (P2, the half D-NUMERIC-INGRESS-STRICT does not gate), `F2-W-06` (P2), the HEEx halves 2k/2l hand over | **folded** (2026-09-21, `claude/c2-elixir-3`, `e0abfe2c1..5e2fb7328`) |
| 2n backend adapters + cross-backend residue (batch 3) | Opus | `src/generator/dotnet/**`, `src/platform/hono/**`, `src/generator/typescript/**`, `src/system/migrations-builder.ts` + `sql-pg*`, `src/ir/**`, `src/language/validators/**`, `src/generator/_*/**`, one file per backend per row | **M-T2.17** (D-DAPPER-ALTER: the phase-⑨ ALTER chain for dapper + mikroorm → `dapper-unsupported`, `mikroorm-unsupported`, ledger `dapper-no-schema-evolution`), `find-predicate-unsupported` (prove unreachable or name the shape), `polymorphic-id-ref-unsupported` (default: D-POLYMORPHIC-ID-REPRESENTATION + build on M-T5.7's reader), `workflow-handle-unsupported` (owner-only D-HANDLE-REMOVAL — re-measure, report), ledger `M-T1.11-domain-floor-message-code` (P2), `F2-W-09`, `F2-W-08`, `F2-EXPR-7` (D-FIRST-ON-EMPTY ruled), `F2-W-14`, `F2-W-12`, `workflow-projection-rename-unexpressible`, `M-T5.7-inheritance-tail`, `M-T5.19-a`, `M-T5.5-stdlib-tail`, `M-T3.15-E2`, the IR two-spellings mission | **folded** (2026-09-21, `claude/c2-backends-3`, `f90e5d78f..3547371f7`) |

Batch 1 = the five backend trees (2a–2e); batch 2 = 2f–2j once batch 1 is folded (2f edits the same backend files the five packets own, so it runs after them).

## Fold notes

- **2m folded fourth and last of batch 3** (`claude/c2-elixir-3`, six signed commits `e0abfe2c1..5e2fb7328` plus its merge of the 2k fold `b475de6a1`; `origin/main` unchanged at `918b2c9fd`). Two conflicts, both composed: `MAX_OPEN_GAPS` — 2m wrote 20 → 17 on its own tree (2k's seam + its two moves) while the branch already carried 2k × 2n at 18, so the fold pins **16** with every packet's comment in order (2n 19 → 18, 2m 18 → 17 and 17 → 16); the ledger `.md` (generated region, regenerated; the JSON auto-merged — open **118** / done **171**). `LATENT_SEAMS` 27. Closed with code, proved on a BOOTED LiveView against a real Postgres (7/7 ExUnit: page window, both sort directions, the money sort, the pager across three windows, filter narrowing and clearing; the strongest mutation ran on the booted app — reducing `sort_key(%DateTime{})` to the identity failed exactly "sorting by a datetime column orders CHRONOLOGICALLY"): **`F2-MT640-SORT-DEAD` — the ledger's one open P1 — `G2646-open-heex-layout-inert`'s pager arm and `loom.table-filter-unsupported` were ONE defect under three names**: HEEx read `sortKey:`/`sortDir:`/`page:` only when `serverPaged` and never read `filter:`, so a non-paged `find all(): T[]` rendered an unsorted, unpaged, unfiltered table where the other six frontends sort/page/filter. `renderTable` now has two modes — server mode byte-identical, client mode threading the rows through a per-deployable `LoomTable` module (filter → sort → slice, the pager counting the filtered set) with reload-less `loom-sort`/`loom-page` clauses; `table-filter` row `gap` → `seam` with the census re-deriving the "covers every frontend" claim from the grammar. `schemathesis-F11-int32-range` closed for the half **D-NUMERIC-INGRESS-STRICT** does not gate: an elixir `int` publishes `format: int32` + `minimum`/`maximum` and is bounded at both ingress paths (`__loom_int32_param`, `validate_number` per int column); the string-coercion narrowing still waits on the owner. Ruled: `loom.elixir-if-stmt-unsupported`'s four sub-shapes re-classed `scope` under **D-ELIXIR-IF-BRANCH** (two are not elixir-local: a conditional `emit` is an event-ordering question, `opHasProvSite` is target-neutral); `G2646`'s `i18nFormat` arm under **D-HEEX-I18N-FORMAT**. Not built, measured to file:line: `loom.heex-component-host-state-unsupported` (the component walk already produces all four accumulators and `ComponentActionInfo` drops them; the hard half is that a HEEx function component cannot read a host assign, plus a latent un-namespaced-assign bug in the table controls); `F2-W-06` (the `<timestamp>` normalisation is one regex shared by every backend's leg, so the golden re-capture is NOT elixir-only — C5's coordinated moment); the `vanilla-document` residue deliberately NOT re-classed (nobody has measured whether the other four backends refuse the same shapes on a document aggregate). The cycle also caught two harness defects of the §59/§63 shape (a money-sort assertion that could not fail because `NUMERIC(19,4)` loads every value at one scale; a row regex that matched the CSRF token). Two process facts for the next batch: the behavioural elixir leg is not runnable on this host (`run-elixir.mjs` spawns `mix` directly and there is no BEAM outside docker); a fresh worktree has NO `node_modules`, so `npx` hangs silently — `ln -s /home/user/Loc/node_modules node_modules`. Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors (25 warnings, baseline), `test-typecheck` ratchet OK, counts up to date, docs build 0, targeted suites 492 files / 5033 green, the packet's elixir compile leg green on five fixtures, full `npm test` redirected (result in the PR body). Hand-off: [`handoffs/wave-c2-2m-elixir.md`](handoffs/wave-c2-2m-elixir.md). **Batch 3 register result: `MAX_OPEN_GAPS` 20 → 16, `LATENT_SEAMS` 25 → 27, ledger P1 = 0 (open 123 → 118, done 166 → 171), six new proposed decisions (D-PAGE-PRIMITIVE-SHADOW, D-MODAL-CONTROLLED-OP-FORM, D-POLYMORPHIC-ID-REPRESENTATION, D-ELIXIR-IF-BRANCH, D-HEEX-I18N-FORMAT; D-FIRST-ON-EMPTY applied), M-T5.37 minted.**
- **2n folded third of batch 3** (`claude/c2-backends-3`, five signed commits + one `origin/main` merge, `f90e5d78f..3547371f7`; `origin/main` @ `918b2c9fd` — **#2975**, the CI fix that unfroze the merge queue (`generated-flutter-build.yml` pulled Google's retired Android `tools` SDK package on every run and, carrying `merge_group:`, ejected every queue entry for six days) plus two pairwise compile-oracle fixes — merged first, clean). Three conflicts, all composed: the ledger JSON three-way by row id (2l's `feliz-flutter-persist-codec-asymmetry` and 2n's `F2-EXPR-7` both `open → done`; open 121 / done 168, table regenerated), the ledger `.md` (generated region only), and `MAX_OPEN_GAPS` — 2k and 2n each wrote 20 → 19 on their own branch, so the fold pins **18** with both comments (2n's branch had lowered the pin without the file's reasoning comment; the fold wrote it). `LATENT_SEAMS` 26 (2k's), `UNDOCUMENTED_BASELINE` 366 → 365 (2n's). One format-only fix at fold: 2n's new `find-predicate-position-census.test.ts` landed unformatted (biome error; the batch-2 java-test lesson again). Closed with code: **`loom.find-predicate-unsupported` DRAINED — built, not deleted on a claim** (2f suspected it unreachable; it fired on five shapes, four of which also crashed drizzle codegen from `0 error(s)` source, because `firstNonQueryableNode` was position-blind — a predicate with no column in it was admitted and went four ways across five backends; closed target-neutrally by `firstNonQueryablePredicate` plus three emitter arms the census found missing — drizzle and MikroORM reach a value object's flattened bool column, and Dapper was NOT `FULL_SUBSET` as the register claimed: it had no VO-flattened arm at all; the 220-line descriptor module, gate, message, row and two test files are gone, a 17-shape × 4-adapter census replaces them, strengthened to fail on a stub sentinel after its first draft passed with the MikroORM fix reverted); **D-FIRST-ON-EMPTY applied** (ledger `F2-EXPR-7`: node and elixir stopped degrading, RS-36 minted). Ruled: **D-POLYMORPHIC-ID-REPRESENTATION** `proposed` — and it DIVERGES from the plan's default on measurement: no FK and no discriminator, because `migrations-builder`'s M-T4.4 filter already drops the FK and the delegating base reader wants no discriminator; the remaining build is two identity types on java/.NET, not a schema. **M-T5.37** minted for the IR two-spellings class (`.36` was taken — check before minting). Not built, measured: **M-T2.17** — there is no shared phase-⑨ half left to land (`assignMigrationsOwner` is adapter-blind, so the chain is already computed and thrown away at two `hasMigrations` guards); it is two per-adapter appliers, and #2946 already adds the `__loom_migrations` ledger its slice (c) needs; `loom.workflow-handle-unsupported` verified and left alone per the OWNER-ONLY rule, removal blast radius re-measured (12 `src/` files / 44 refs, 7 test files, 3 docs, a grammar regen); the four `F2-W-*` rows re-dispositioned as ONE coordinated OpenAPI-parity moment, M-T1.11 (c) proposed to fold INTO the C5 golden re-capture; `M-T3.15-E2` is wider than its row says (`findAll` is reserved on java only, so the shadowing workaround is refused for every project on one backend's behalf). The full suite earned its keep in-packet: an undeclared `this.isDeleted` lowers with `memberType: primitive string`, byte-identical to a real string column, and two existing pins want opposite answers — a targeted run would have shipped the regression. Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors after the format fix (25 warnings, baseline), `test-typecheck` ratchet OK, counts up to date, docs build 0, targeted suites 907 files / 8198 green, node behavioral leg (packet) 128/0 with 59 wire cases / 0 divergences, full `npm test` redirected (result in the PR body). Three owner decisions asked in the note's §12: the `handle` build-or-remove ruling, whether the `F2-W-*` family becomes one mission, and whether M-T1.11 (c) folds into C5. Hand-off: [`handoffs/wave-c2-2n-backends.md`](handoffs/wave-c2-2n-backends.md).
- **2k folded second of batch 3** (`claude/c2-walker-3`, five signed commits + one `origin/main` merge, `f90e5d78f..79d44b442`; `origin/main` still @ `f90fdd159`, the fold merge clean with no conflict — the 2l fold had already carried the same main). Closed with code on all four JSX/Vue/Svelte/Angular targets: the cross-frontend `Modal { OperationForm { <row>.<op> } }`-inside-a-`For` silent-codegen defect 2h found (`emitModal` walked the form child for its side effect and discarded the result, so a declined op-form still emitted a trigger calling an `open<Op>Modal` the shell never wrote, at 0 errors; `emitFor` now also opens a row scope — the M-T1.33 omission); `loom.frontend-prop-type-unsupported` (`money` → `Decimal` by sentinel so each file keeps its single decimal.js import, `File` and a `valueobject` spelled structurally; Angular's private `angularWireType` had never reached the shared layer and answered `unknown` for all three; `uiUsesMoney` grown so the generated `package.json` declares the dependency) — row re-classed `gap` → `seam`; and the 2g §7.1 ruling, DECIDED and BUILT narrower than the launch's default: `loom.component-shadows-stdlib` refuses a `component` whose name is a walker primitive (a `component Alert(…)` was emitted and never rendered while the pack primitive took the call site) but NOT a `valueobject` (re-probed: `valueobject Money` does not capture the `Money` primitive, and refusing it would have rejected `sales-system.ddd`) — **D-PAGE-PRIMITIVE-SHADOW** `proposed`. Ruled, not built: `loom.modal-controlled-op-form-unsupported` — `open:` must be honoured, **D-MODAL-CONTROLLED-OP-FORM** `proposed`, and the build is a pack-layer packet (every pack's op-form module owns its own trigger and needs a trigger-less twin; `designs/shadcnVue/v1/form-op-module.hbs` is still a `TODO(vue-forms)` stub) so it was deliberately not part-landed. `loom.table-filter-unsupported` verified clean on the four (only phoenixLiveView excluded), HEEx half to 2m. The `slot` seam re-measured: react/vue/svelte already render slot params correctly, Angular refuses honestly and 2h's tag work is the enabler — an Angular-local job, not a cross-frontend seam. Not reached (2g's measurements stand): `frontend-collection-op`, `toast-message`, `page-form-locals`, `scaffold-filter-param`, `ui-projection-read`, ledger `M-T1.10-handler-vocabulary` / `F2-CFE-10`. Two things to read in the note: the corpus scan behind D-PAGE-PRIMITIVE-SHADOW covered `.ddd` files only and the full suite found five test fixtures declaring `component Badge` / `component Chart` inline in `.ts` template literals (renamed, the gate not narrowed; no shipped `.ddd` collides); and a product hole with no mission — a list page cannot host a per-row operation dialog on ANY frontend (it now says so instead of emitting broken code; making it work needs the row hoisted into its own component). `MAX_OPEN_GAPS` **20 → 19**, `LATENT_SEAMS` 25 → 26. Eight fixes mutation-proved by file copy with the failing assertions quoted. Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors, `test-typecheck` ratchet OK, counts up to date, docs build 0, targeted suites 741 files / 7261 green, full `npm test` redirected (result in the PR body). Hand-off: [`handoffs/wave-c2-2k-walker.md`](handoffs/wave-c2-2k-walker.md).
- **2l folded first of batch 3** (`claude/c2-selfhost-3`, eight signed commits `aa415bdfd..b87ed395f` on the merge of `f90e5d78f`; `origin/main` @ `f90fdd159` — #2906, #2962, #2959, #2914 — merged first, both merges clean, no conflict). Closed with code: the ledger row `feliz-flutter-persist-codec-asymmetry` (the F# `optional` persist codec at every tier — the two self-hosting persist tables now agree on every TYPE, the one residue is a TIER with a measured cause), a pre-existing Feliz silent-codegen defect the build surfaced (a bare assignment into an optional store cell emitted FS0001 from a clean `.ddd` — the third member of 2i's family, pinned), and the `toast(…)` half of `loom.flutter-action-body-unsupported` (M-T1.32, the one P1 in the register: `lib/toast.dart` + a `GlobalKey<ScaffoldMessengerState>` on `MaterialApp`, the twin of the `navigate` bridge; gate arm, catalog entry and firing fixture deleted, the three refusal tests FLIPPED). Measured rather than built: `loom.async-effect-subject-unsupported`'s population is exactly three reachable subjects of which ONE (awaiting a workflow) is buildable — the row cannot reach zero by building and re-classes `scope` when that lands (the register `what` and four pinned cases now say so). Deliberately NOT re-classed: `loom.store-lifetime-target-unsupported`'s record-shaped residue ships on four of six frontends, so a `scope` would be dishonest by the register's own rule — the note's §4 carries the build recipe instead. Left with recipes (two resized against the emitter): M-T1.32's standard-op `match await` half, M-T1.34, the feliz component-host async effect, the feliz component-deferral shapes (the optional-param slice is an M, not an S — the body READ needs unwrapping too), `F2-CFE-11`'s flutter half (call-site-only: `pack.ts` already translates `testid:` → `Key` and `forms-emit.ts` already emits `{super.key}`; still needs 2j's `find.byKey`-vs-DOM-id shape call). `MAX_OPEN_GAPS` unchanged at **20** (both rows survive with a strictly smaller `what`), `LATENT_SEAMS` 25. Proofs: `dotnet fable` + `vite build` on a showcase carrying every optional scalar at `local` and `url`; Flutter SDK re-fetched after the restart (3.47.3 → **3.47.5**, recipe in the note) — `flutter analyze` clean, three widget tests proving a real `SnackBar` from a Notifier, the CI showcase's 9 `flutter test` cases green. The packet's full `npm test` was redirected and read: 4 reds, all the worktree-structural `packaging-split-*` family (verified, not assumed). Fold gates on the merged tree: langium drift 0, `tsc -b` clean, biome 0 errors, `test-typecheck` ratchet OK, counts up to date, docs build 0, targeted suites 156 files / 1530 green, full `npm test` redirected (result recorded in the PR body). Hand-off: [`handoffs/wave-c2-2l-selfhost.md`](handoffs/wave-c2-2l-selfhost.md). The 2m launch followed this fold (BASE = this fold's head).
- **Batch 2 merged** (#2933 → `main` @ `5049b6fea`, 2026-09-14 16:44Z; the branch restarted from that commit for batch 3). From the ready flip to the merge: one real red (`corpus × java` on 2f's `projection-implicit-sub.ddd` — the reactor's immutable-key setter, fixed at both `dispatch.ts` call sites with a mutation-proved gate and a local Gradle compile), four `origin/main` merges (#2927, then #2864/#2950 + 34 siblings, #2915/#2922, #2917 — the last two after `MERGE_CONFLICT` ejections when those PRs landed ahead; the register `site:` lines drifted three times and the register's own site gate caught each; the one code composition was #2917's React page-shell import beside 2g's), a container restart that cost only the poll, and ~2.5 h from the last push to the merge. **Batch 3 rationale.** The wave's exit is `MAX_OPEN_GAPS` → 0 and ledger P2 = P3 = 0; batch 2 left **20** gap rows, every one with a mission or a `D-*`, plus 1 P1 / 5 P2 / 32 P3 ledger rows (most of the P3s size L — product missions that C6 builds by track). Batch 3 fences the DRAINABLE remainder by tree again (2k walker/JSX frontends, 2l feliz+flutter, 2m elixir, 2n backend adapters + the cross-backend IR residue) and dispositions the rest into named missions so the register can reach 0 honestly. **Wave C3 is not claimed by this coordinator**: its territory is being worked by the CR1 wave (#2938, the verification layer, batch 2 blocked on the `src/ir/**` fence this PR just released) and the testability fleet (#2964, six PRs already open) — both cited, neither duplicated; C4 (seams; #2770 + #2778 are in) and the C5 moments follow C2's close.
- **Post-flip fix (batch 2).** The ready flip lit ONE red on `25c2beeac`: `corpus × java (Spring Boot)` on 2f's new `projection-implicit-sub.ddd` — the in-process reactor rendered `state.setOrderRef(e.orderRef())` for the saga row's immutable `@EmbeddedId` (javac: cannot find symbol), because `emit/dispatch.ts`'s two reactor call sites never threaded the correlation field into `javaWorkflowStmtTarget`'s `fixedKeyField` — the rule the command-workflow facade already had (M-T6.62). Invisible until D-PROJECTION-IMPLICIT-SUB made an uncarried reactor dispatch at all, and equally wrong for a carried one whose body spells the correlation rule as an assignment. Fixed at both sites; `test/generator/java/reactor-correlation-key-assign.test.ts` pins both consumer shapes × both triggers (mutation-proved: 4/4 fail with the pre-fix emitter restored by file copy); the fixture compiles under the local JDK 25 + Gradle 9.6 (`compileJava` exit 0). Every other leg on that head was green (96 of 97 completed checks).
- **#2927 composed** (`origin/main` @ `fb8222065` merged after the 2f fold): the overlap 2h predicted in `src/generator/angular/form-fields.ts` — 2h's field-error/ARIA helpers and #2927's `controlTsType` + array-placeholder block sat in one hunk, both kept; #2927's new `fieldInput` array branch inherits the `testid` string that now carries the ARIA bindings, so the two compose without a second edit. Full fast suite on the completed batch-2 tree before this merge: 2081 files / 24462 tests, 0 failures; after it: `tsc`, `biome ci`, both counts, the angular + system + e2e-route + validation + CLI + platform suites (278 files / 3856 tests).
- **2f folded fifth and last of batch 2** (`claude/c2-ir`, seven signed commits `dbb71d516..6e3aaf71a` on `118d0a3f7`; `origin/main` unchanged since the 2j fold at `d743e0a98`). Six conflicts, all pins and registers, none in code: `MAX_OPEN_GAPS` — 2f's −4 (the three config-shaped rows renamed out of the `-unsupported` suffix: `context-filter-no-principal`, `datasource-binding-missing`, the unreachable `ui-realtime-unsupported#backend-serves-no-sse` arm deleted; `sensitive-wire-unsupported` re-classed `scope` under **D-SENSITIVE-INSPECT-ONLY**) lands on the 24 the batch stood at → **20**, every comment kept; `LATENT_SEAMS` **25** (one row re-classed `seam`, auto-merged); three register `site:` lines re-pointed at the raisers the merged `workflow-checks.ts` moved (the site-resolution gate caught each); the ledger three-way by row id (open 123 / done 166, no row edited on both sides); `docs/decisions.md` keeps every entry; README + ledger counts regenerated. 2g's lowering hunks in `lower-expr.ts`/`lower-types.ts`/`lower.ts` merged without a conflict. Built, mutation-proved: `loom.function-block-no-return` descends into `if`/`else` path-sensitively (and a silent IR purity-sweep hole one level behind it closed); **D-PROJECTION-IMPLICIT-SUB** applied — `deriveEventSubscriptions`'s early return meant a `projection … on(e: E)` over an uncarried event folded on NO backend, boot-proved both directions on generated node/PGlite (a node-only fold-filter trap fixed on the way); M-T5.14's reading-service handle reaches dotnet/java/elixir (three per-backend fixes, plus missing imports for pure service calls on java/.NET and a missing repo-read arm in `inferExprType` that typed `let hits = Repo.find(...)` as `string`); the drizzle projection-membership crash closed target-neutrally in `firstNonQueryableNode`; M-T3.16 C4 goldened per route on all five (already correct, a missing golden). Dispositioned: the five unowned `scope` rows (three → new **M-T5.36**, `domain-service-read` → M-T5.14, `if-stmt-page-body` → M-T1.20 under new **D-PAGE-BODY-EXPRESSION**). Not built, measured: `polymorphic-id-ref-unsupported` (needs a representation D-tag first), M-T1.11 (c) domain-floor `code` (an L with a golden re-capture), M-T3.16 C2 (rides C5's re-capture), the IR two-spellings class (censused on all five, not byte-identical — its own mission), F2-W-06 (one blocker stale, the rest an elixir-tree change after C5). Found while compiling: a python identifier collision in #2923's family, handed to that wave. Three owner decisions asked in the note §12.
- **2j folded fourth of batch 2** (`claude/c2-flutter`, thirteen signed commits `96d298c29..82f67e251` on the merge of `fb8f86730`; `origin/main` @ `d743e0a98` — #2923 — merged first, clean). Ten conflicts, every one a 2i × 2j composition on the two self-hosting frontends' shared seams, none picked: `MAX_OPEN_GAPS` — 2j re-classed `flutter-async-effect-unsupported` `gap → scope` under **D-FLUTTER-COMPONENT-BINDINGS** (owner new **M-T1.34**), so on top of 2i's split the fold pins **24** (all four comments kept); the `store-lifetime-target-unsupported` register row's `what` now carries BOTH halves (feliz drained toward the union by 2i, flutter `json` + nullable cells by 2j, a nullable cell still refused at the `url` tier for a measured reason) and its `site` re-pointed to the raiser the merged `store-checks.ts` moved (the register's own site-resolution gate caught the drift); `store-checks.ts` keeps 2i's `walkStmtsDeep` import beside 2j's tier threading; `persist-codec-divergence.test.ts` keeps 2i's drained rows plus 2j's `json`-agrees and optional-scalar pins; the ledger JSON three-way by row id with the THREE rows both packets narrowed composed by hand — `M-T1.16-invariant-validation-feliz-flutter` and `sourcemap-feliz-flutter-not-emitted` moved to `done` (both halves landed, the real landing on both), `feliz-flutter-persist-codec-asymmetry` narrowed to the one divergence left, the optional scalar the F# table lacks (open 127 / done 162); `docs/decisions.md` keeps both appended entries; T1's M-T1.20 carries 2h's Angular table AND 2j's Flutter table with the new M-T1.34 section; T3's M-T3.9 keeps 2i's stronger correction; the testing plan's sourcemap row names both frontends. Built with proofs on a compiled and RUN Dart app (a local Flutter 3.47.3 SDK at `scratchpad/fl/flutter`): `--sourcemap` records Flutter pages (`file()`) and each pooled component (`fragment()`), proved end-to-end with `ddd breakpoints`; M-T1.16's flutter half through the same `takeSingleFieldChain` gate with the now-exported `singleFieldMessage` (five frontends deny with one sentence); M-T4.12 (1) under D-FLUTTER-BEARER as a drop-in `api_client.dart` façade (cookie on web, bearer on native; a `'Bearer \$token'` escaped-dollar defect `flutter analyze` cannot see was caught by running the app); the a11y leg scans one case per page (1 → 8 on the showcase); the persist codec gained nullable and `json` cells; `KNOWN_FLUTTER_GAPS` registered in `allowlist-ratchet` at `max: 0`. Measured: the ten deferred component shapes split by the decision into a `ref`-backed half (M-T1.34, recipes in M-T1.20) and a permanently refused route-`id` half. Closed by measurement: M-T1.18's M-G seam is a no-op; M-T3.9's Flutter claim was stale; `F2-CFE-11`'s flutter half is bigger than its `S` (flutter forms have no key surface) and is recorded with a recipe. **Process lessons the note records:** a piped `npm test … | tail` returns `tail`'s exit code and can never fail; and two suite names one character apart (`packaging-split-discovery` vs `packaging-split-fs-discovery`) turned a spot-check into a false all-clear — the fold's own full run is the number that counts. Its #2937 overlap (`messages.ts`, different keys) composed without a hunk.
- **2i folded third of batch 2** (`claude/c2-feliz`, six signed commits `4d8f67b34..3531d2a2f` on the merge of `118d0a3f7`; `origin/main` @ `c677f2426` — #2924 — merged first, clean). Three conflicts, all composed: `MAX_OPEN_GAPS` — 2i's +1 is a row SPLIT (the target-agnostic half of `loom.feliz-async-effect-unsupported` became `loom.async-effect-subject-unsupported`, raised for every mounted ui; no gap drained or added), so on top of M-T5.34's +1 and 2h's −1 the fold pins **25** with all three comments; the ledger JSON three-way by row id (open 129 / done 160, no row edited on both sides); the ledger `.md` (2i's `M-T1.16` row narrowed to the flutter half, the `queryview-lambda-int-plus-literal-concat` line 2i still carried as open dropped because 2g closed it, counts regenerated). Built with mutation proofs: the feliz navbar gates an entry on its page's `requires`; `store-lifetime-target-unsupported#field` drained its feliz half to the flutter table's union and exposed a silent-codegen defect underneath (an `enum`/`datetime`/`guid` `state {}` cell emitted F# that could not Fable-compile from a `.ddd` reporting `0 error(s)` — fixed in `type-fs.ts`, pinned on an in-memory store); `--sourcemap` records the feliz frontend via `fragment()`; M-T1.16's feliz half through the shared `takeSingleFieldChain` admission gate (byte-identical when no invariant translates); F66's `classifyFelizAsyncEffect` promoted — the identical model was refused on Feliz, `0 error(s)` on React (then `await Promise.reject(...)`) and threw at codegen on LiveView. Measured, not built: the ten deferred component shapes are nine and collapse to three fixes (one is the already-`claimed` `F2-FFE-4` currying row). **M-T3.9's premise was stale** — the scaffolded History section already ships on all seven frontends; the mission text was corrected. Its #2896 overlap (five diagnostics/registry files, append-shaped) had already landed on `main` and merged without a hunk conflict. Process lesson recorded in the note: a test committed before it was ever executed (`test/ir/async-effect-subject.test.ts`) failed to parse its own fixture and only the full suite caught it.
- **2h folded second of batch 2** (`claude/c2-angular`, `118d0a3f7..78edec729`, five signed commits; `origin/main` @ `76ef74add` — #2896 — merged first). Two conflicts, both composed: `MAX_OPEN_GAPS` — main's M-T5.34 minted `loom.workflow-handle-unsupported` (24 → 25) while 2h re-classed `component-children-unsupported` to `scope` (24 → 23), so the fold pins **24** with both comments; `docs/new-plan/README.md` regenerated by `mission-counts --write` from the merged trackers. Built: the walked Angular user component is invoked by its own kebab tag with its class in the page's standalone `imports: []`, so call-site children project into `Slot { }` / `<ng-content>` — the `ngComponentOutlet` drop survives only for `extern` (the selector belongs to the author), re-classed under new **D-ANGULAR-EXTERN-CHILDREN**, successor **M-T1.33**; M-T1.12 slice 6's Angular half (the remedy diverges by pack — material's `<mat-error>` vs primeng/spartanNg spelling `aria-invalid`/`aria-describedby`; proved in a browser on built bundles, mutation-proved). Verified already shipped and were stale docs: M-T1.14 (archived `done`) and M-T1.11 `modalTitle`. The 15 `KNOWN_UNREACHABLE` pins re-verified — one (`primitive-modal`) had a wrong reason its fixture could not catch; both fixed. Not built: the four deferred component shapes, re-measured real with per-shape recipes in M-T1.20 (`action(T)` is one Angular-local hoist; `slot` needs a `_walker` seam — handed to the walker tree, 2g having already handed off, so it is a C3 row; the input-fed read needs a thunk-taking api client). Cross-frontend hand-off recorded: `Modal { OperationForm { <lambda>.<op> } }` inside a `For` emits undeclared identifiers on react/vue/svelte at 0 errors — a 2g-tree row for C3. **Open-PR overlap to compose when #2927 lands:** `src/generator/angular/form-fields.ts` (`fieldInput`'s branch list) and `walker/page-shell.ts`. Local leg note: `ng build` passed under node 24 in docker; the host node is below the Angular CLI floor. 2j launched from this fold's predecessor `fb8f86730` at the same time.
- **2g folded first of batch 2** (`claude/c2-walker`, `118d0a3f7..84daaee8e`, six signed commits; `origin/main` @ `1cf156a42` — #2899, #2925 — merged first, both merges clean). Closed with code: `queryview-lambda-int-plus-literal-concat` (two facts, not one — the page-body read's `data:` lambda bound its parameter at the `string` placeholder, AND the project-global ambient entity index was empty for the `subdomain { context { … } }` layout; both mutation-proved, correct arithmetic on all six frontends incl. the feliz/flutter hard build breaks) and M-T1.28's F50 as THREE instances (the store-reading `derived`, the action-body `toast(...)`, an `extern` called only from an action body losing its import; `svelte-check --fail-on-warnings` + `vite build` pass on the fixture carrying both shapes). M-T1.8 vue error boundary and M-T1.3 Angular `Chart` verified already done. Six rows measured, not built, each with file:line (§1–§6 of the note): `modal-controlled-op-form-unsupported` needs a ruling (the gate's "supported" targets silently drop `open:`); `page-form-locals-unsupported` is a 69-template refactor across 10 packs; `frontend-collection-op-unsupported` and `toast-message-unsupported` are target-agnostic by design (part-landing re-introduces the carve-out); `scaffold-filter-param-unsupported` and the keyed/folded projection read span other packets' trees. `MAX_OPEN_GAPS` / `LATENT_SEAMS` unchanged. **Two decisions escalated to the owner (§7):** whether a user declaration shadows a page primitive in a ui body (the ambient-index fix was narrowed to aggregates to avoid ruling — a `valueobject Money` would otherwise capture the `Money` primitive), and the `Modal { open:, OperationForm }` bar. **Fence note for 2f:** this packet's lowering half lives in `src/ir/lower/{lower-expr,lower-types,lower}.ts` (2f's fence) — compose at 2f's fold. The packet's §10 signing worry is a verification artefact (the container's `.pub` is empty so `%G?` reads `N`); every commit carries an SSH signature.
- **Batch 1 merged** (#2907 → `main` @ `9713ffa18`, 2026-09-14 05:34Z; the branch restarted from that commit for batch 2). The merge took three more `origin/main` merges after the post-flip push — #2865/#2903 (ledger three-way, README counts), #2902/#2883/#2870, #2873/#2884 (clean), #2900 (one import hunk in `java/emit/auth.ts`, both imports kept) and #2872/#2926 (one waiver hunk in `ir-walk-census.test.ts` where each side had retired a different adjacent entry — both dropped; the census gate is the proof) — each gated locally before its one push (langium drift 0, `tsc`, `biome ci`, both count checks, the targeted suites; the full fast suite on `aa6a4408f`: 2049 files / 23979 tests, 0 failures). **Queue mechanics worth knowing (for `docs/ci-gating.md`):** the queue is serial with `max_entries_to_build: 1`, so an entry waits behind every group ahead of it and each group costs ~25 min of runner time; a queued PR's branch is LOCKED against pushes (GH006) and neither disabling auto-merge nor converting to draft releases it — only closing the PR dequeues it (then push, reopen, re-flip ready, re-enable auto-merge); a head that conflicts with a merge that landed ahead of it is ejected with `MERGE_CONFLICT` only when its own group is built, so pre-merge `main` the moment a conflicting group passes rather than waiting for the ejection; and a red group ahead (#2917's own new test failing against main after #2878) rebuilds every group stacked on it. Total wall-clock from the post-flip push to merge: ~10.5 h, of which the branch spent ~6 h queued or locked.
- **Post-flip fixes (the heavy legs the draft tier never ran).** The ready flip at 19:09Z lit four per-backend legs the packets' own runs could not reach, every one on a fixture batch 1 added or extended — and every one a defect the compile tier is blind to. (i) `java-reserved-words.ddd` (2d) — `synchronized := this.strictfp(boolean)`: the EXPLICIT self-call lowered to a `method-call` on a `this` receiver where the bare `strictfp(x)` lowers to a `call` with `callKind: "function"`, so python rendered `self.strictfp` against the def-site `_strictfp` (`AttributeError`, behavioral-python) and elixir `record.strictfp(x)` on a struct (corpus-elixir `--warnings-as-errors`). Fixed at lowering (`lower-expr.ts`, the method-call construction): `this.<fn>(…)` / `this.<op>(…)` now produce the bare form's `call` node — node/java/.NET byte-identical, gated by `test/ir/this-self-call-lowering.test.ts` (mutation-proved, 4 of 5 fail with the arm removed; the fifth is the byte-identical control). A bonus: the elixir `loom.vanilla-op-call-position` gate scans for `callKind: "private-operation"` and could not see the dotted spelling before. (ii) The same fixture on behavioral-elixir — `derived throws: int = this.synchronized + 1` was silently OFF the wire (`expected undefined to be 4`) because `wire-serialize.ts`'s `derivedRenderable` declined every `member` on a `this` receiver; the bare `synchronized + 1` is a `this-prop` ref and projected. Fixed in the walk (a `this.<derived>` read still declines — the renderer inlines only the ref spelling), gated by `vanilla-wire-derived-this-member.test.ts`. (iii) `audit-history.ddd`'s audited RETURNING `settle` (2c) on .NET, EF and Dapper alike (corpus-dotnet ×2, behavioral-dotnet, behavioral-dapper): `intOrNotFound` is CS0104-ambiguous in the audited handler, which imports `Domain.Orders` AND `Application.Orders.Responses` (the snapshot builders) — the wire twin carries the same bare name. The audited handler now spells the Domain union in full; the unaudited control stays bare (`audited-returning-op-handler.test.ts`; `dotnet build /warnaserror` in the leg's own `sdk:10.0` image). (iii-b) The same elixir compile leg had a second `--warnings-as-errors` failure behind the first: `find byLabel(final: bool, label: string): Ticket[] where this.label == label` declares a parameter the predicate never reads (it exists for its wire spelling), and the relational find head bound it unread — the head now derives every parameter's name from the rendered body exactly as it already did for the threaded `current_user` (arity unchanged; `vanilla-find-unused-param.test.ts`). (iv) `tph-crossings.ddd` (2e) on behavioral-java: Hibernate refuses `@SQLRestriction` on a SINGLE_TABLE subclass, so the soft-deletable TPH concrete's filter blew up at boot (`AnnotationException` out of `AnnotationBinder.bindClass`) — legal Java, invisible to the compile leg. The fragment is now hoisted onto the abstract root, discriminator-guarded (`(kind <> 'Parcel' or (not (is_deleted)))`), the java twin of the EF adapter's discriminator-guarded root filter; `tph-subtype-sql-restriction.test.ts` + the booted app (`run-java.mjs tph-crossings`: errored before, 1 passed after). **Hand-off for batch 2 (2f, `src/ir/**`):** (i) and (ii) are one defect class — a construct spelled with an explicit `this.` lowers to a different IR node than its bare spelling (`member`-on-`this` vs `this-prop`/`this-derived` ref; the call half is closed here). Normalising `this.<prop>` at lowering changes the TS emission (`this._x` backing field vs `this.x` getter), so it needs its own byte-identical-or-justified gate rather than a post-flip patch; until then every walker that special-cases `this-prop` must also accept `member(this, …)`.
- 2a folded fifth and last of batch 1 (six doc conflicts, no code conflict): T6's M-T6.14 heading composed (`partial` + DEBT-12 closed), 2a's M-T6.56 blockquotes placed above main's #2852 `done` heading for M-T6.57, both archive sections and both decision batches kept, ledger three-way (`static-subpath-405-node-only` → done), counts regenerated. 2a's gate sites outside its fence: `if-stmt-checks.ts`, `datasource-checks.ts`. Its harness finding — the whole-leg elixir compile invocation dies on fixture 1 behind one long-lived hex mirror while a fresh mirror per fixture works — is recorded in its note for C3/T9.
- 2d folded fourth (pushed as the day's second push, `904e35f57`; the `UNDOCUMENTED_BASELINE` pin 368 → 367 was the predicted 2c × 2d interaction). Conflicts: `java/emit/workflow.ts` (main's #2886 `payloadNames` argument and 2d's `jid()` mangling on the same line — composed), the register pin (2d's 27 → 26 lands as **24**), the ledger (three-way: `M-T6.36`, `F2-ADP-7` → done), the wave log and the counts. 2d's edits outside its fence are the drained gates' own sites (`backend-syntax-checks.ts`, `system-checks.ts`, `validate.ts`, `util/naming.ts`).
- 2b folded third (nine conflicts, all composed rather than picked): `MAX_OPEN_GAPS` — 2b and 2c each wrote 27 → 26 on their own branch, so the fold pins **25** with both comments; the `find-predicate-unsupported` register row's `what` carries 2c's mikroorm narrowing text and 2b's `DAPPER_SUBSET = FULL_SUBSET` fact; pairwise F12 — 2e had narrowed it to dotnet and 2b had dropped the dotnet half, so the entry met the fold empty and is **deleted: `COMPILE_WAIVERS = []`**, one of the wave's exit criteria; M-T6.35's heading takes 2b's re-homing (`#migrations` + `#schema-*` → M-T2.17) above 2c's mikroorm bullets; ledger three-way by row id (`dapper-tenancy-hierarchy-unsupported` → done); the two decision entries appended. M-T2.17 and M-T6.72 are free on `main` (checked).
- Post-hand-off fixes, both found by the packets' own full-suite runs after hand-off and landed as one commit each on their branches: 2e `e1f2d7bb5` (every derived route of `tph-crossings.ddd` gets a caller — the api-caller census ratchets on any fixture with a `test e2e` block; wire golden re-captured 12 → 22 requests, python leg re-matched) and 2c `2c7babfa0` (the MikroORM membership commit had rewritten the whole predicate walk; the three dropped behaviours are restored and only the membership arm moved, the three obsolete narrowing pins now assert both directions) + `e00208add` (`UNDOCUMENTED_BASELINE` 369 → 368 after retiring the audited-returning code — the shrink-only pin was left slack). Lesson both notes record: an emitter change's blast radius includes the module-named unit test and the corpus censuses, not only the feature-named suites.
- 2c folded second. Conflicts: `docs/decisions.md` (both packets appended entries — kept both), the ledger JSON (three-way by row id; `M-T5.14-reading-service-readport-not-threaded` composed: 2e's python-closed row plus 2c's node re-verification note, targets dotnet/java/elixir) and the ledger `.md` (regenerated). **Composition 2c × 2e:** 2c's D-EMBEDDED-TPH refuses at phase ④ the embedded × TPH crossing 2e's F13 fix emits; 2c's refusal wins (it is the language-layer call 2e's D-TPH-BEATS-SHAPE explicitly deferred), 2e's emitter ordering stays as the floor mirroring phase ⑨, and `test/generator/python/tph-embedded-storage.test.ts` was deleted (its fixture no longer validates). 2c's gate-site edits outside its fence (`src/language/validators/inheritance.ts`, `src/ir/util/find-predicate-capability.ts`, `src/ir/validate/checks/{storage-inheritance,system}-checks.ts`, `validate.ts`) are the register rows' own gates and were reviewed at fold.
- 2e folded first, out of the plan's letter order: the trees are fenced, the fold was conflict-free on both merges (`origin/main` @ `9a8f2fe0` — #2866/#2885/#2895 — then `claude/c2-python`), and holding a verified packet for the alphabetical one gains nothing. The register is untouched by 2e (none of its six rows was a `loom.*` gap).
- **2b .NET / EF / Dapper** — branch `claude/c2-dotnet`, `29db198c1..01a25eb20` (+ a merge of `origin/main` @ `9a8f2fe00`). Hand-off: [`handoffs/wave-c2-2b-dotnet.md`](handoffs/wave-c2-2b-dotnet.md). `MAX_OPEN_GAPS` **27 → 26**. Built: dapper hierarchical tenancy (`#deep-scope` + the `DAPPER_UNSUPPORTED` entry + its ratchet pin, all to zero), M-T5.7's unnamed cross-aggregate residue, M-T6.14's missing correlation gate; drained the stale pairwise F12 dotnet waiver by measurement. Dispositioned: `#migrations` + `#schema-*` → new **M-T2.17** (D-DAPPER-ALTER already ruled; it needed the mission), `tph-filter-unsupported` → `scope` under new **D-TPH-SUBTYPE-FILTER**, owner **M-T6.72**. **Three things for the coordinator:** (i) the `G2646` dotnet arm is a NO-OP — measured — so packet **2f** can land `enrichments.ts:1303` alone (and must delete `loom.projection-event-uncarried` with it); (ii) the only file shared with an open PR is `emit/dapper.ts` (#2852), and this packet's hunks there avoid the find/retrieval bodies #2852 works in; (iii) `test/system/gate-ledger.test.ts`'s vacuity guard had to move — `DAPPER_UNSUPPORTED` was the one populated register keeping it honest, and this packet drains it. `npm test` on the folded tree is green (the only residual reds are three `packaging-split-*` tests that cannot pass in ANY git worktree — no `node_modules/@loom` workspace symlinks — proven by recreating them, plus one contention timeout that passes alone).
