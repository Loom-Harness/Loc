# Refresh wave — 2026-09-10 (make `docs/new-plan/` true again before the completion waves start)

*Claim doc for one Opus wave. Base: `main` @ `42fce9e` (2026-09-10, #2770 Wave 2 and #2848 merged) **plus PR #2849** (the completion-waves plan; docs-only, green, in the merge queue — owner decision 2026-09-10). This wave IS packet **C0.4 + C0.5** of [`../completion-waves-2026-09.md`](../completion-waves-2026-09.md), expanded into tree-fenced Opus packets, with two additions that plan does not carry (R6 register↔mission truth, R7 the hollow-work sweep over the 100 merges since the last refresh). It forks no status table: every status flip lands in the track file that owns it; ledger rows stay in the ledger; register rows stay in `unsupported-register.ts`.*

## Why a refresh wave, measured on `bc7ed8f`

| symptom | measured | rule it breaks |
|---|---|---|
| README "Last refreshed" | 2026-09-02 @ `36d8516` — **519 commits / ~100 merges ago** | README §5 "the only status table" |
| `done` headings still in `T*.md` | **20** (T1 ×5, T6 ×2, T8 ×8, T9 ×5) | "a track file should never carry one for long" |
| README live count | says 153 (09-06); `grep -c '^## M-T' docs/new-plan/T*.md` = **175** | §91 a count in prose is a cache |
| merged-but-unflipped missions | M-T1.28 #2786, M-T5.26 #2788, M-T5.27 #2789, M-T6.53 #2787, M-T1.29/M-T1.30 #2830, M-T5.29 #2833 — still `in-flight`/`open` | RUNBOOK §5 |
| duplicate id | `M-T6.60` = two live headings (numeric strictness + workflow `create`); #2849 renumbers the second to **M-T6.62**, #2850 still claims `M-T6.60` in its title | `unsupported-register.test.ts` one-heading rule |
| `M-FT` field-test series | 18 distinct ids in the merge log (1 2 3 4 6 7 10 11 12 13 18 19 20 21 22 26 27 31), **zero headings** anywhere under `docs/new-plan/` | every id resolves to one heading |
| the two older 2026-09 plans | `improvement-waves` (W0/W1/W2 executed — #2752, #2770; 2.6 → #2778) and `verification-waves` (G0–G3 executed — #2766) carry no outcome block; their §1 fences list PRs merged a week ago | a plan that reads as live routes agents at finished work |
| README "Open PRs at this refresh" | lists #2678/#2674/#2672/#2671/#2720/#2721/#2723/#2728/#2729/#2713/#2730/#2628 — all merged or closed | same |
| ledger `open` | `node scripts/ledger-counts.mjs` = **153** (P1 7 · P2 13 · P3 32 · P4 89 · P5 12); #2849 §1 says 156; last reconciliation ran repros on 09-03 | "a ledger's `open` bucket is a claim, not a fact" |
| nightlies on `main` | **four** red 09-08 → 09-10: Conformance full, Frontend full-stack e2e, Schemathesis, Playground e2e (#2849 §0 counts three) | — |

Everything above is docs/ledger drift, not code drift: the code moved, the map did not. Agents route on the map (CLAUDE.md §Sync — two duplicate slices in one session were built off stale status), so the map is repaired **before** C1–C7 dispatch.

## Incoming-PR fence (read before claiming)

| PR | state | what it does to this wave |
|---|---|---|
| **#2849** completion-waves plan | in the merge queue | **Merges first (decided).** It already: renumbers `M-T6.60`→`M-T6.62`, README 153→175, replaces the 09-02 open-PR list, drops M-T3.4 from §Sequencing. Do not redo those four edits. This wave branches from its head if it has not merged by kickoff, and rebases once when it does. Its §4 C0.1 merge order is already stale (#2770, #2846 merged) — R4 corrects it. |
| **#2778** hotspot splits (12k lines, 35 files, rebased on `bc7ed8f`) | ready, CI running | Relocates 35 `site:` line refs in `unsupported-register.ts` and every `file:line` into `system-checks.ts` / `ui-checks.ts` / `mikroorm.ts`. **Rule for every packet: cite `file#function`, never `file:line`, into those three files; touch no `site:` field.** R6 lands after #2778 or by function name only. |
| **#2843** M-T9.55 give-up glob (13 files) | ready | On merge: M-T9.55 → `partial` (glob fixed, 26 sites routed; the 28-site drain named by C1 1d is the residue). R2 flips at fold time, not before. |
| **#2848** playground typing race (test-only) | **merged** (`42fce9e`) | One of #2844's two failing specs is closed; R4 records the flaky-gate state as *measured by `scripts/flake-budget.mjs`*, never by hand. |
| **#2850** M-T6.60 → M-T6.62 workflow `create` | draft, claim only | Coordinator comments once: retitle to **M-T6.62** after #2849 merges. Its fix, when it lands, flips M-T6.62; not this wave's row. |

## Packets (all Opus; one draft PR; sub-branches `claude/refresh-<packet>`)

Rules: §3/§3a of `improvement-waves-2026-09.md` and rules 10–18 of `completion-waves-2026-09.md` apply. Specific to a docs wave: **no status flip without reading the PR diff against the mission's acceptance line** (a PR that *mentions* an id is not a PR that *closes* it — #2780 found 11 of 26 "fixed" rows fixed on some targets only); **no count in prose** — a number is a script or it is deleted; **no `file:line` into a file an open PR moves**.

| packet | rows | tree fence | exit |
|---|---|---|---|
| **R0 coordinator** | branch off #2849's head; open the draft PR with this file as the body; fold R1–R7 in the order below; after every fold run `npx vitest run test/system/unsupported-register.test.ts test/system/coverage-guarantee.test.ts test/system/archived-docs-fence.test.ts test/system/diagnostic-docs-anchors.test.ts test/system/ddd-source-census.test.ts test/system/inline-ddd-source-census.test.ts`, `node docs/build.mjs`, `npx biome ci .`; one push per day; comment on #2850 (retitle) | the PR body, `waves/refresh-wave-2026-09-10.md` (this file — hand-off notes appended) | one green PR; #2849 merged before it |
| **R1 status truth, T1–T8** | (i) the seven merged-but-unflipped: read each PR's diff vs the mission's acceptance line; flip `done` **only** where every target the mission names is covered, else `partial` with the residue named (known residue: M-T1.28's Svelte F50 + `renderToast` seam; M-T5.27's `locator-matcher-receiver` consolidation); (ii) move the 15 `done` headings in T1/T6/T8 to `archive/T<n>-done.md` verbatim + their `missions/M-T*.md` briefs to `archive/missions/`; (iii) every heading whose body says "claimed by #NNNN" where #NNNN is merged/closed: re-derive the state; (iv) M-T6.62 (#2850 draft) → `in-flight (#2850)`; M-T6.61, M-T6.57 stay `open` | `docs/new-plan/T{1,2,3,4,5,6,7,8}-*.md`, `archive/T{1..8}-done.md`, `missions/`, `archive/missions/` | 0 `done` headings in T1–T8; every flip cites PR + evidence line |
| **R2 status truth, T9 + the testing-quality plan** | the most-churned file (25 commits since 09-02, 39 headings): the 5 `done` headings (M-T9.36, 9.37, 9.44, 9.45, 9.49) archived; stale "claimed by" lines (M-T9.14 #2663, M-T9.35 #2647, M-T9.29 #2512, M-T9.21 #2664) re-derived; bodies #2766 outran (M-T9.22 FUZZ-1, M-T9.17 slice 2, M-T9.18 MACRO-1, M-T9.28 slice 2, M-T9.40's self-contradiction) rewritten to what shipped; M-T9.38 `open` (unblocked — M-T1.21/22 merged); M-T9.50's count line → "see `test-typecheck-baseline.json`"; M-T9.55 flipped at fold time per #2843; `testing-quality-improvement-plan.md`'s three done entries cross-linked to the archive | `docs/new-plan/T9-toolchain-health.md`, `archive/T9-done.md`, `testing-quality-improvement-plan.md` | 0 `done` headings in T9; no `MAX_OPEN_GAPS`-style number narrated in prose |
| **R3 the `M-FT` register** | reconstruct one row per id from the 16 merged field-test PR bodies (`git log --grep='M-FT'`, titles `m-ft-<n>-…`): id, title, PR, what shipped, residue; land `archive/FT-done.md` (merged ids) and a **hand-off list** of the ids the log cannot reconstruct (the owner's field-test session holds them) — the coordinator inserts one live heading per unmerged id into the owning track **after** R1/R2 fold, or `declined` per the owner | `docs/new-plan/archive/FT-done.md`, `archive/README.md` (one row) | every `M-FT` id in the merge log has one heading; unknown ids listed for the owner, not invented |
| **R4 the plan spine** | README: replace the stacked "Last refreshed" notes with one dated block (older notes → `archive/refresh-log.md`), the live-count line and the track table's Live/Archived columns computed by a new `scripts/plan-counts.mjs` and pinned by `test/system/plan-counts.test.ts` (mutation-proved: change a count in the README, the test fails); §Sequencing numeric-series paragraph rewritten (the four are merged, M-T9.36 done); §Priority shortlist rows re-verified against the registers (row 2's M-T6.35 survivors re-counted from the register, not from prose); `improvement-waves-2026-09.md` and `verification-waves-2026-09.md` each get a dated **Outcome** header (what merged, in which PR; which items were re-homed into `completion-waves` §4 by id) and their §1 fences are deleted; `completion-waves-2026-09.md` §4 C0.1 rewritten to the PRs actually open (#2778, #2843, #2848, #2850), §0 "three flaky legs" → four nightly reds incl. Playground e2e, §1 ledger 156 → the script's number; RUNBOOK gains rule "cite `file#function` into any file an open PR splits" | `docs/new-plan/README.md`, `RUNBOOK.md`, `improvement-waves-2026-09.md`, `verification-waves-2026-09.md`, `completion-waves-2026-09.md`, `archive/refresh-log.md`, `coverage.md`, `scripts/plan-counts.mjs`, `test/system/plan-counts.test.ts` | README carries no hand-tallied count; both older plans read as executed; `completion-waves` §4 C0 matches the PR list on the day it merges |
| **R5 ledger truth (C0.5)** | for each open P1 (7) and P2 (13) row: **run the repro** on the head (generate + read output, the #2840 method), not read PR bodies; close what reproduces nowhere, correct `remainingTargets` where it reproduces on a subset; fix the four rows the 09-10 survey found stale (`F2-CB-C7` targets vs `remainingTargets`; `static-subpath-405-node-only` is elixir-only; `M-T3.8-sensitivity` half-closed by `loom.sensitive-wire-unsupported`; `F2-MT640` ownerless since M-T6.40 archived); disposition every P4/P5 row (101) as `done` / `open` / `declined(reason)` so the bucket stops carrying rows nobody reads; `scripts/ledger-counts.mjs --check` fails on a row with neither a PR nor a disposition | `docs/audits/targets-completeness-2026-08-30.ledger.json`, `docs/audits/targets-completeness-2026-08-30.md` (header counts), `scripts/ledger-counts.mjs` | `open` = rows that reproduce today; P1 rows each name a live mission or a C1/C2 packet |
| **R6 register ↔ mission truth (the "no fake claims" packet)** | every `gap`/`scope` row in `unsupported-register.ts` cites a live mission that still describes that gap (24 LIVE per #2849 — re-derive); every mission that claims a gap has a register row, a pin, or a ledger id; `KNOWN_FLUTTER_GAPS` re-counted from the array (the plan says 4 — verify by reading the file), `E2E_LESS_CORPUS_FIXTURES` (13), `COMPILE_WAIVERS` (4), `NON_PARSING_SOURCES` (1), flowbite `KNOWN_DEVIATIONS` (5 rows citing merged #2750 → re-owned by M-T1.12 or drained) each cross-linked both ways; `docs/generators.md` §"What the generators don't do" (pagination and the `X id` typeahead ship — delete) and the `parity-auditor` skill's `target-matrix.md` (four-frontend era, cites the deleted `ashPhoenix` pack) rewritten **from the register/pin sets**, not from memory. **Owner/mission fields only in the register — no `site:` edits (#2778 owns them).** | `src/diagnostics/unsupported-register.ts` (mission/owner fields), `docs/generators.md`, `.claude/skills/parity-auditor/references/target-matrix.md`, `test/generator/_packs/pack-spacing-contract.test.ts` (owner line), `test/generator/flutter/parity-freeze.test.ts` (comment/owner lines) | every register row ↔ mission link resolves both ways; both docs regenerated from code-side sets |
| **R7 hollow-work sweep (M-T9.8) over the 100 merges since 09-02** | the adversarial read the README asks for after any large multi-agent push: for every merged PR since `36d8516` that flips a status or deletes a waiver, check (a) the gate it cites reaches the thing it names (§59/§63 — grep the assertion, seed one defect where cheap), (b) no waiver outlived its fix, (c) no `TODO`/give-up landed in emitted output (`generated-output-sentinels`), (d) no test was softened (`.skip`, widened pin, `continue-on-error`) in the same diff; each finding → a `docs/audits/hollow-work-sweep-2026-09-10.md` row with PR + file + the failing shape, and a mission id handed to R1/R2 for insertion (never a fix — this packet edits no `src/`) | `docs/audits/hollow-work-sweep-2026-09-10.md` (new), `coverage.md` (one row) | every merged status flip since 09-02 has been read against its gate; findings minted, none raced |

**Fold order:** R4 (spine) first so every later packet writes into the corrected README/plan shape → R1 ∥ R2 ∥ R5 ∥ R6 ∥ R7 (file-disjoint) → R3's live headings and R7's minted missions inserted by the coordinator last → one rebase if #2849 or #2778 merged meanwhile → flip to ready.

**Cost:** 8 Opus agents (coordinator + 7), 3–5 days of queue time. CI-cheap by construction — `docs/**` paths skip every generation leg; the one `scripts/` + `test/system/` file pair fires only the fast set. Per `verification-waves` §7.2, keep prose-only follow-ups batched into the next fold, never pushed alone.

## Exit (measured, on the merged head)

- `grep -cE '`done`' docs/new-plan/T*.md` → 0 headings; every `M-T`/`M-FT` id in any commit since 08-17 resolves to exactly one `## ` heading under `docs/new-plan/` (`unsupported-register.test.ts` extended to check the merge log, or a one-off script whose output is in the PR body).
- `node scripts/plan-counts.mjs` output equals every count the README prints (test-pinned).
- `node scripts/ledger-counts.mjs` `open` bucket contains only rows whose repro reproduces on the head; the P1 list equals the C1 1e row list in `completion-waves` §4.
- Both older 2026-09 plans carry an Outcome block; `completion-waves` §4 C0 names only PRs that are open on the day.
- `hollow-work-sweep-2026-09-10.md` exists with every finding minted as a mission or recorded as "gate reaches its target, verified".

## What follows this wave (the prioritized programme — `completion-waves-2026-09.md` §4, with this wave's corrections)

1. **This refresh wave** (R0–R7) — the map is true.
2. **C0 residue** — the four nightly reds and the three `flaky-gate` issues at the root (C0.2), the `pr-gate` mechanism measured once (C0.3, M-T9.57). Nothing later can be mutation-proved from CI while a promoted gate is red on 60 % of PRs.
3. **C1** — the P0 miscompiles (M-T6.62 via #2850, M-T6.61, M-T6.57) and the silent class (give-ups/TODOs/throws without a `loom.*` code; ledger P1 = 0).
4. **C2 ∥ C3** — one Opus packet per target tree draining every LIVE register row, Flutter form fields, deferred component shapes, pairwise waivers (implement on every target that has the feature, or a `D-*` scope decision — never silence); in parallel the test-only verification wave (E2E-less 13 → 2, compile-only cells 59 → ≤ 10, the Flutter/Feliz runtime leg, the emitted-source authz census, the five legs that never gate).
5. **C4** — seams and compiler hygiene after #2778: `test/` typecheck 470 → 0, the ~130 uncoded validator conditions, callable unification, the `RouteTarget` decision.
6. **C5** — the 15 owner rulings in one `docs/decisions.md` PR (48-hour defaults), then the three coordinated breaking moments (A4 `getById`, `denyByDefault`, `organizationContext`).
7. **C6** — every PRODUCT mission built or owner-dispositioned; **C7** — docs truth, the closing generate-based `parity-auditor` sweep and the closing hollow-work sweep, two green weekly deltas on #2580.

## Hand-offs

*(appended verbatim by packets as they land)*
