# Wave C1 — the P0s and the silent class (coordinator log)

*Plan: [`../completion-waves-2026-09.md`](../completion-waves-2026-09.md) §4 Wave C1. Base: `main` @ `d619291` (Wave C0, #2863, merged 2026-09-11 ~09:0xZ). The nine packets ran as Opus agents in isolated worktrees on local `claude/c1-<packet>` branches; the harness cut every worktree from `origin/main` @ `bcc1c4c` (main *without* C0), so each packet's track-file and ledger edits met C0's at fold and were composed by hand. One PR for the wave, on `claude/loom-review-planning-adz0n4` restarted from `main` per the merged-PR rule.*

## Status: **eight of nine packets folded** — 1d-ii (§18 sentinels + M-T1.31 F17) still running (2026-09-11)

## Reconciliation before launch (2026-09-11)

Rows already owned elsewhere, folded by reference: #2850 (1a receiver/correlation fix — packet 1a stacked on it; both merged now), #2852 (1c `envelope`, option B — nothing left in 1c; still open), #2860 (1d M-T1.31 F11; still open), #2857 merged (1b M-T6.61 + 1f M-T6.54 F19), #2699 / #2705 merged (1f M-T5.25, M-T6.51 — tracker flips only), #2752 merged (1f M-T6.50 (1)). Owner-gated, not launched: M-T6.58 (`D-HANDLE-REMOVAL`).

## Packets

| packet | model | tree fence | rows | state |
|---|---|---|---|---|
| 1a workflow create | Opus | `src/ir/util/workflow-own-state.ts`, `_workflow/**`, five workflow emitters, validator + messages, corpus | M-T6.62 second spelling (assignment) on all five + .NET member name + elixir collector on `walkWorkflowStmtChildren`; `loom.workflow-create-correlation-unsupplied` (+`#payload`); rule-13 fixture; M-T6.58 repro only | **folded** — [`handoffs/wave-c1-1a-workflow-create.md`](handoffs/wave-c1-1a-workflow-create.md); #2850 told on its thread |
| 1b placement fork | Opus | `src/language/validators/stmt-placement.ts`, `_stmt/target.ts`, messages, fixtures beside the gate | M-T5.28 three codes per `D-FOR-IN-DOMAIN` (phase ④ — the constructs have no IR node); M-T5.30 minted; M-T6.61 flipped (already fixed by #2857) | **folded** — [`handoffs/wave-c1-1b-placement-fork.md`](handoffs/wave-c1-1b-placement-fork.md) |
| 1d-i give-up drain | Opus | `_walker/**`, six targets, heex, gate test | `giveUp(target, code, text)` — 81 sites / 23 files carry a catalogued code, 14 uncoded HEEx declines routed, `UNCODED_GIVE_UPS` empty; the walker-invariant test (58 primitives × 7 targets); M-T9.55 `done` | **folded** — [`handoffs/wave-c1-1d-giveup-drain.md`](handoffs/wave-c1-1d-giveup-drain.md); hand-offs H1–H5 |
| 1e-i ledger backend rows | Opus | per row's `remainingTargets`, `money-scale.ts`, ledger | F2-XB-4, F2-CB-C7, F2-CB-C1, 40-digit money range check fixed with cross-backend gates; G2644 verified; F2-MT640-SORT-DEAD re-diagnosed (HEEx client-side sort/paging parity gap, S → M) | **folded** — [`handoffs/wave-c1-1e-ledger-backend.md`](handoffs/wave-c1-1e-ledger-backend.md) |
| 1f validator drops | Opus | `ir/validate`, `language/validators`, java `repository.ts` (F18), elixir (M-T6.55), python | M-T6.54 F18 on both java read surfaces + the fail-OPEN twin (a soft-deleted row readable through `findAll()` once any find said `ignoring softDeletable`); M-T6.55 three shapes emitted (+`loom.vanilla-op-call-actor`); M-T5.25 / M-T6.51 flipped + archived; M-T6.50 narrowed | **folded** — [`handoffs/wave-c1-1f-validator-drops.md`](handoffs/wave-c1-1f-validator-drops.md) |
| 1g uncoded-condition ratchet | Opus | `test/system/diagnostic-*`, `src/diagnostics/**` | M-T9.56 gate half: 129 → 128 uncoded sites across 12 files, per-file shrink-only baseline, no-`loom.unknown` firing assertion, `UNDOCUMENTED_CODES` length pin; one site drained as proof | **folded** — [`handoffs/wave-c1-1g-uncoded-ratchet.md`](handoffs/wave-c1-1g-uncoded-ratchet.md) |
| 1e-ii ledger flutter rows | Opus | `src/generator/flutter/**`, ledger | F2-CFE-1 (`lib/nav.dart` bridge, shared `tryRenderNavigateCall`, `loom.flutter-action-statement-unsupported`), the four `KNOWN_FLUTTER_GAPS` built and the pins deleted | **folded** — [`handoffs/wave-c1-1e-ledger-flutter.md`](handoffs/wave-c1-1e-ledger-flutter.md) |
| 1h C0 hand-offs | Opus | `hono/v4/observability-builder.ts`, elixir (E5), java workflow boxing, java corpus legs' gradle daemon, `.claude/skills/**` | c7 pino browser envelope; E5 was every scaffolded Phoenix filter bar 500ing (the HEEx `match` arms lost which read they named — `QueryBinding` carries `retrieval` + `gate`); RS-26 boxing of java workflow params; daemon on two of three legs (pairwise refused with reason); `ashPhoenix` scrubbed; **M-T6.69 / M-T6.70 minted** (renumbered from the packet's 6.63/6.64 — taken by #2882/#2881) | **folded** — [`handoffs/wave-c1-1h-c0-handoffs.md`](handoffs/wave-c1-1h-c0-handoffs.md) |
| 1d-ii §18 sentinels + F17 | Opus | walker-core, riverpod, flutter-target, drizzle, extern-functions, component-prop-type, svelte routes-emitter, liveview-emit, elixir domain-service-emit; `ui-component-deferral-checks.ts` | the §18 list; M-T1.31 F17 | **running** (stacked on 1d-i) |

## Fold notes

- Fold order: 1d-i, 1a, 1b, 1e-i, 1f, 1g, 1e-ii, 1h (1d-ii pending). Every conflict was in `docs/new-plan/**` or the ledger (C0's restructures vs the packets' main-based edits) except two additive code hunks (`code-docs.ts`, `gate-ledger.test.ts`).
- The ledger JSON was merged semantically twice (C0's `open` array as the base, the packet's row moves and re-diagnoses applied by id), then `ledger-counts.mjs --write`: open 140 → **135**, P1 6 → **1** (`F2-MT640-SORT-DEAD`), done 148 → 153.
- Compositions the packets could not see: 1e-ii's four `giveUp` calls in the riverpod emitter gained 1d-i's mandatory code argument; three new e2e-less corpus fixtures registered (`projection-fold-statements`, `paged-nonrelational`, `workflow-primitive-params`); the F2-CB-C7 guard pinned in the authz census with a minted `importGateOnly` reason (the predicate cannot refuse — the import is the subject); `loom.flutter-action-statement-unsupported` given a docs anchor instead of joining `UNDOCUMENTED_CODES` (1g's length pin holds at 369); M-T9.56's heading re-spelled with a legend status.
- `scripts/mission-counts.mjs` did not exist on the packets' trees (it is C0's); every hand-off notes it; the README region is regenerated at fold.
